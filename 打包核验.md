# 打包核验范围

本次只打包既有装修 Skill 和成果，不重新设计房间，不修改原始设计文件。

打包程序将逐文件写入 SHA256 清单，并在压缩结束后重新读取 ZIP 内的全部文件验证字节数与 SHA256。若任一文件不匹配，程序退出失败，不作为完整交付。运行结果和 ZIP 总校验值以交付回复及旁边的实际核验记录为准。

当前主卫源模型上一轮验证：59版连续通路校核、原门扫掠、旧版入口及窄屏页面检查通过。58版因连续通路瓶颈被否决。以上均不是施工验收。

解压后当前主卧/主卫页面的 HTTP 加载、模块/JSON 请求和浏览器错误，将在交付前抽查；不重新运行全部历史脚本，也不把原机器硬编码路径的脚本称作即装即用。

第三方可执行依赖由原项目原样保留，本次不运行其中的构建工具。可浏览页面需要 Python 3 本地 HTTP 服务与支持 WebGL 的浏览器；重新离线渲染另需 Blender。
