# 13 层 / 01 户：建筑 CAD 核验记录

日期：2026-09-05。本轮只提取图纸证据，未据不完整的转换图改动住宅模型。

## 后续本地处理进展（取代之前“需要找人转 T3”的阻塞结论）

无需用户联系图纸提供者。本机已另行使用 ODA File Converter 27.1（官方站下载，数字签名 Valid）对 DWG 副本转换。

ODA 保留了 3052 个代理实体，但天正墙体等代理仍只有 8 字节空预览，不能仅靠显示代理图形解决。本轮进一步读取原始对象字段：

- `recover_drawing.py`：恢复 469 个墙体对象的端点及两侧厚度；705 条文字及插入点。原对象不覆盖，衍生对象放在独立 RECOVERED 图层。
- 103 个门窗开口的中心、宽度、高度及方向通过“唯一对应同块墙段、与墙平行、中心落在墙体几何中线、宽度落在墙段范围内”检查。这里考虑了墙体左右厚度不对称引起的中线偏移，以及 DWG 零标高的微小负浮点误差（容差 0.001 mm）。目标大横厅图块 AX-120-COMP-WALL 的 11 个此类开口均有唯一墙段匹配。门扇铰链/开启方向及其它窗块亦不等于已完整核验。
- 恢复墙线在匹配的开口范围内断开，避免把门洞画成实墙；仅处理本轮新增的诊断图层，不删除原 CAD 对象。落地高开口只表示通口，不凭空指定门扇方向。
- 解析不是官方天正导出器。数值范围校验、墙段对应与视觉匹配只能支持诊断用途，不能证明所有字段语义或施工精度。
- `diagnostic-recovered.dxf`：诊断恢复文件，不是完整 T3 交付文件。
- `13层大横厅-CAD核对.png` / `.svg`：13 层对应标准层图二的右端大横厅，旋转至与《1号房.pdf》相同的展示方向。左右房间、厨房、双卫、入户及阳台的排列已进行视觉对照。01 门牌来自用户说明，不能把图块名称中的 01 当作门牌证明。
- 可核对的数值例：客餐厅沿横向两端相关墙中心 y=5450 / 11750 mm、相向厚度各 100 mm，推导该段净跨度 6100 mm；PDF 对应标注 6090 mm，存在 10 mm 差异。保留差异，不静默覆盖 PDF 尺寸或声称完全一致。
- 原文件 SHA256 需保持与下文一致。网页住宅模型本轮未重新定稿或覆盖。

设备过滤得到重要反证：按名字搜索会找到 117 个“空调外机”图块引用，但加入父图块、图层关闭/冻结、实体不可见过滤后，这类命名图块在所选标准层中没有可见实例。因此不能拿这些旧图层符号推定机位、数量或购买规格，仍须匹配正式可见图例和套管符号。

用户确认的阳台中柱继续保留。开口恢复后，客厅与阳台交界处两处开口之间留下短墙段：同一墙基线 x=33850 mm，上下开口边界 y=8600 / 8950 mm，中间保留长度 350 mm；墙体两侧厚度各 100 mm。该处在诊断图中约为 350×200 mm 的墙段，**是否对应用户所说的承重柱、其真实结构截面与装饰完成尺寸，尚未确认**，不能直接代替现有模型假设的 450×450 mm 柱，也不能拆除。玻璃外立面中央与客厅/阳台分界是不同位置，不能混同。

## 原始文件与读取边界

- 文件：`C:/Users/vv-dev-work/Desktop/AC-6-P-平面图.dwg`。
- SHA256：`35EF04BE9159FAD7063FDC33384F8DDB38B707CA2437630C9DD5FBEC5BBC89F8`。
- 文件版本 AC1024；原文件未修改，未上传外部服务。
- 本地 LibreDWG 0.14 导出 `building.dxf` 和 `building.json`。读取器不识别 TCH_WALL、TCH_OPENING、TCH_DIMENSION2 等天正对象。DXF 渲染缺墙体、门窗及标注，不能作为完整建筑底图。
- `AX-6-BZC1.png` 仅是标准层图一的兼容性检查图，不是 13 层交付图。
- `tch-text.json` 是自定义二进制文字提取的候选集，含误识别，不能全部视为已核实内容。下列楼层整串文字与同块逐项楼层文字相互印证；仍不得用文字恢复结果外推墙体几何完整性。

## 已找到的楼层对应

| 图名 | 图纸标注楼层 | DWG 图块 |
| --- | --- | --- |
| 标准层平面图一 | 6、9、12、15、18、21F | AX-6-BZC1 |
| 标准层平面图二 | **7、10、13、16、19、22F** | **AX-6-BZC** |
| 标准层平面图三 | 8、11、14、17、20、23F | AX-6-BZC3 |
| 标准层平面图四 | 24、27、30F | AX-6-BZC1-1 |
| 标准层平面图五 | 25、28、31F | AX-6-BZC-1 |
| 标准层平面图六 | 26、29F | AX-6-BZC3-1 |

证据定位：

- `texts.json`：图名“标准层平面图二”，模型空间属性 handle `27FC7`，坐标约 `(52588.804, -175746.454)`；对应 AX-6-BZC 图块插入点 `(0, -178200)`。
- 原 DWG 对象 handle `5DC5B`（十进制 384091）：`（7、10、13、16、19、22F）`；归属图块 AX-6-BZC。
- 同图块 handle `5DC4A`（384074）：`13F`。
- 同图块表内还有 `35.100` 标高文字；尚未完成原版表格逐行视觉复核，本记录不据此定义室内净高。

用户确认的“01 大横厅户型”不能与图名“标准层平面图一”混同。后续本地恢复已定位楼层组和与 PDF 对应的房间排列；**实际机位与现场尺寸仍未确认**。

## 已读到的空调相关说明

标准层共用说明图块 `Ax-TL-50` 的普通文字可直接提取：

> 空调室外机搁板冷凝水管采用 Ø50UPVC 管，每层预留三通，具体做法详水施。

位置：图块内 y 约 `-25936.536`，由同一行数个文字对象按 x 坐标顺序拼合。

> 所有空调套管及热水器排气管未注明处均表示洞中心距墙面 150。

位置：图块内 y 约 `-22996.536`。

还读到图例文字“卧室预留预留%%c80空调套管;孔中距地2380mm”（原文如此）及另一项“%%c80空调套管;孔中标高距地2330mm”。这两项属于不同图例，**未对应 01 户具体符号前不得任选一种用于模型**。

另一条原文：`家具及厨卫洁具布置仅为示意。` 因而原图家具不是必须照搬的装修布局。

## 尚不能从当前读取结果确认

- 建筑 CAD、PDF 和现场净尺寸之间的差异及地理朝向。用于展示的旋转关系已与 PDF 核对。
- 01 户专属外机平台所在侧、平台净尺寸、容纳设备数量及散热/检修条件。
- 用户确认须保留的阳台中柱，其实际截面、位置、结构属性与包饰尺寸。
- 各空调孔与冷凝水三通位置；实际外机选型、冷媒管长、高差、排水坡度。
- 当前 3D 模型的墙体、柱子和设备位置是否与该建筑 CAD 一致。

因此，当前网页中空调室内机/吊顶仍属方案示意；外机和管线不应被固定。不得把“用户猜测在阳台侧”升级为图纸确认。

## 当前剩余核验

继续利用现有原文件，核对门扇开启方向、其它窗块、可见设备图例、套管符号和尺寸；不再把“另找人转换”作为继续分析的前置条件。

若进入真实施工，建筑图通用说明本身不能替代给排水/暖通定位证据或现场复测；当前不固定冷凝水路线、设备容量及阳台中央柱尺寸。
