## 2026-09-09 corner-59（通道优先）

spec新增cornerCandidate，保留56/57/58坐标与对比。默认corner；master-corner-bath.js负责左右换位后的洁具、L吊装浴帘与连续圆盘路径；master-bath-corner-geometry.mjs让2D/3D共用帘线，测试复用距离计算。无结构、真实管线、外部系统变更。浴帘仅挡水，不是密闭分隔，不绘制未经核实的下水口或假设抬地。首轮试验的双面内折门因内部操作圆盘受挤压而撤销，最终不交付该硬隔断做法。沿原页面更新，浴帘收起/展开、俯视与尺寸平面同源。通过bathLayout参数可对比旧版，58页面明确标记44cm瓶颈已否决。

## 2026-09-09 separate-58（只隔独立淋浴）

新增spec.design.bath.separateCandidate与master-separate-shower.js，由原hotel模块在完成共同盆柜后分派。默认bathLayout=separate，hotel保留57版，original保留56版；不覆盖前两版候选坐标。马桶按z向重新生成完整网格。折门两块405mm名义门板，第一块绕左端向内转0—85°，第二块反向折回，模型进深约40cm、展开口净通行目标约70cm；真实联动轨道、铰链、防夹与应急开启待厂家验证。2D与3D折门均由同一参数计算，俯视仅显示隔断截面，勾选使用空间显示马桶/洗漱包络。原外门控制面板在主卫剖视中隐藏，避免被误读为马桶上方的室内面板，物理位置不改。无外部接口或结构拆改操作。

## 2026-09-09 hotel-57（主卫酒店式两区）

原spec新增design.bath.hotelCandidate，保存洁具、站位、两条功能路线及隔断/门参数；原bath.fixtures保留。默认运行时套用候选坐标，bathLayout=original返回56版原位置。新增master-hotel-bath.js负责侧向盆、镜柜、分区移门、右侧淋浴及标注；原模块跳过旧卫浴网格，避免新旧叠放。2D、3D与俯视剖切均使用同一candidate矩形；原CAD墙/开口/床不变。移门厚30mm、行程890mm，位于隔断湿区一侧，避免撞原凸出墙。俯视隐藏高隔断但保留底部轮廓并说明非矮墙；镜柜内部为剖视，不是开门模拟。原主卫入口效果仍剖视隐藏门侧墙/门；俯视显示原门。无下水孔、横管、排污泵或结构施工操作。

## 2026-09-09 service-56（主卫与衣帽梳妆）

新增master-service-areas.js挂载同一scene及annotations；fullMirror/tray参数进入原spec.design.serviceAreas。全身镜挂到中间门组，随内部剖视隐藏；理物板行程300mm，不隐藏柜门冒充抽出。三只衣帽外抽底高改750/990/1170mm、总高174mm，为950mm理物板留位。主卫90×45cm盆柜占地保留，镜柜约13cm深，增加淋浴L形浴帘轨及可收布帘。镜柜内部显示明确为剖视而非开门。使用三项独立控制：tray-pull、bath-mirror-inside、bath-curtain。几何不改变原门窗/床/洁具位置，不新增下水点或结构孔；镜面采用旧环境近似反射，非精确光追。可仅回退本模块、挂载、spec新增字段及衣帽抽屉改动，无外部接口、设备连接或发布。

## 2026-09-09 split-door-55（上下分段柜门）

修改原master-window-ac-option的spec、三维脚本、HTML与integrated-details模块。直柜340cm、入口端15cm，总宽355cm、深60cm不变；四个85cm名义分区，单扇门宽42.1cm。上排八扇柜门高49cm，下排按长衣、短衣外抽、叠放、短衣外抽组织；两组各两只外抽，抽出30cm。入口原15cm范围内采用R150内凹弧面，平面与三维复用同一多边形。新增仅开上柜状态，幕布放下时强制关柜并锁定控制。原墙、门窗、床及空调位置不改，无外部接口变更；回退仅限本轮分格和入口端差异，保留52版。

## 2026-09-09 integrated-54（原三维效果内深化）

新增master-suite-integrated-details.js，挂载到原master-window-ac-option.js的scene。替换旧衣帽柜组，三个500名义分区；新外抽沿+x运动280mm；保险箱与工具尺寸仅代理。新增R40平面台面、R20书架前角、R100镜形、灯和面板网格、单眼皮和卫浴天花。S05/P01经可见性检查改到柜侧950高，不再埋在镜柜后；旧独立标注图保留但不再作为本次交付入口。

HTML新增就地标签开关、综合柜剖视和抽屉开关。标签每帧随camera投影；关闭立即隐藏父层，小屏最多两条。entry/storage/whole视图仅隐藏原入口门扇，bath视图隐藏门侧遮挡；原开口spec和真实门组保留，caption明确剖视。入口镜面使用局部立方环境近似，不是精确实时镜面。回退范围仅新模块及原页面挂载差异，不重置用户文件；没有外部接口和设备连接。原spec的2600净高仍是旧假设，界面不再把机顶3cm当现场事实。

## 2026-09-09 suite-detail-54（独立设计标注）

build-suite-detail-54.cjs 读取53版规范的原房间多边形、门窗和家具外轮廓，生成静态HTML、四张SVG与54版点位JSON；9组灯、6处开关、9个电源/设备功能点、8类圆角。单独编号，排除旧位置的梳妆、空调及取消的展示格灯，原3D未同步新柜内深化。生成PNG为同源SVG的3200×2264浏览器截图，不是生成式写实效果图。无外部接口、真实设备控制或新依赖；原文件不改，可返回53版。

## 2026-09-09 window-ac-option-53（独立方案B）

最新覆盖：ac.r=[5.10,1.35,5.35,2.30]，管线从右端z2.30起；书柜低段50cm宽/顶2.23m，上段收进x5.49后为36cm宽/2.25–2.60m，新增机身/墙带/书柜高度碰撞检查。两帘boxEnabled=false，取消包盒实体，改3.5cm宽顶装轨，base2.54/高2.5cm，电机base2.18m。布帘长边止于z1.96、纱帘1.99，轨道不撞书柜；没有因此消除机顶3cm不足的结论。以下最初包盒描述已被本段覆盖。

新增master-window-ac-option.html/.js、-spec.json、-audit.js，从52版结构/床/主卫原坐标派生，原页面、spec及模块不修改；共享只读three/投影模块。梳妆r=[.02,2.12,.47,3.12]，衣柜r=[.02,3.12,.67,4.62]，床尾r=[1.95,2.769,5.35,3.369]。二维与三维读同份spec，界面提供全套、柜墙、梳妆、窗上候选和坐姿通路；SVG可见性通过toggleAttribute处理。

模型新增照片可见的高处窗上墙带，其室内侧位置/厚度属于待实测假设，不新增地面墙线，不虚构可打孔的结构性质。挂机95×25×30cm，base2.27、顶2.57，暂定层高2.60/窗顶2.25；机顶3cm，小于沿用的暂估20cm检修/进风预留，installation.ready始终false。穿管沿窗右侧缓降后至墙外短段终止，虚线与环标志只指候选，不创建真实钻孔或室外机。布帘轨试移飘窗内x5.66，纱帘x5.89；帘盒/机型/梁底仍待复核。没有外部服务/数据写入，回退只需返回52版链接。

## 2026-09-09 closed-taper-52（保留原方案）

spec新增footCabinet.taper及家具真实梯形末端polygon，删除niche；同源footFront函数驱动柜侧/层板/门轴及斜抽前。主门宽43.6cm，末列门投影42.8cm、实际约45.2cm，restAngle约-18.82度；闭门边界按斜前缘检查，开启扫掠保留逐度检测。抽屉沿原z方向拉出30cm，盒后部36×36cm、前部梯形连接示意，非已选定五金。75cm桌板同时做该列固定隔板，不重复叠加层板；层板1.15/1.70m，保留2.01m顶部被褥层。3D/平面同步封闭末列、台面相切过渡与梳妆侧衬；更新文案和局部测试。无API/外部发布，回退限52版局部差异，不更改既有房间结构。

## 2026-09-09 continuous-corner-51（历史）

vanity.top.polygon统一驱动2D/3D连续台面，75cm面高、3.5cm厚，从44cm柜列贯通120cm妆台；两段R7.5cm反向圆弧在15cm水平距离内完成15cm深度退让，两端与直边相切。末端柜侧板仅在凹格范围断开，旧矩形桌板和装饰连接条移除。衣物深抽移至111.2–130.8cm，上下门/层板同步调整；另一浅抽仍为65–71.2cm，柜门及抽面均暖白。新增join近景，所有视图从同一spec生成。台面贴墙点采用1e-7m容差的边界包含判断，未扩大实际房界；沿用原inside算法的其他检查不改。无外部接口/发布，回退限本轮局部差异。

## 2026-09-09 aligned-drawer-band-50（历史）

所有door.accent=false，仅衣柜drawer0保留accent。drawer0 base=.65/h=.062/travel=.25m；drawer1 base=.754/h=.196/travel=.30m。storage下门高.566，上门base=.954/h=1.471m，层板随抽屉重新定位。柜门及深抽暖白，妆台两只浅抽同衣柜浅抽暖灰；取消桌后额外带状板，转角收口保持同标高。浅抽内部隔盒同步降低，抽屉间增白色固定收口条填补无用空口。44cm列宽与43.6cm门宽写入主视角说明。其余结构不改，无外部发布，局部差异回退。

## 2026-09-09 five-bay-storage-49（历史）

spec.bays=[1.95,2.83,3.71,4.15]；四扇长门＋两片功能柜短门实际宽均.436m，长门高2.345m，内部88/88/44cm。功能柜两抽面宽43.6cm、盒外宽36cm、深36cm、行程30cm，基高.754/.954m，整列accentFinish=#cec5b9。门抽材质同源，columnCount=5区别于六片门板。叠放功能区缩窄、挂衣区加宽，不声称容量全面增加。顶层宽挂衣区放被褥，窄功能区用小件换季箱占位。门开启90°局部余28.3cm；安装、真人取衣及长门铰链仍未验收。恢复本地8768服务仅绑定127.0.0.1，未外部发布；回退按本轮局部差异。

## 2026-09-09 distributed-drawers-48（历史）

footCabinet.drawers缩为2只宽抽、撤销中隔板；storage上门base1.154/top2.425m。vanity.shallowDrawers两只面宽39.2cm、盒外宽36.4cm、盒深33.3cm、总高6.2cm，base.65/top.712m，避让桌底.715m与后支撑；行程25cm。侧抽沿原占地重做实际抽盒，侧柜由实心占位改侧板/背板/下门，行程30cm。增加两个独立控制项，2D抽拉范围/占道提示同步，非真实五金。审计浅抽高度、家具扫掠及收凳条件；人体腿部、滑轨承载、防夹与下门五金未验证。无外部操作，局部差异回退。

## 2026-09-09 handleless-band-47（历史）

spec去除upper独立门；长衣/短衣门高234.5cm，storage上门107.1cm，门顶均242.5cm，固定顶封条保留。reachesQuilt决定取被褥时打开哪扇大门。六抽分三排，base=.754/.954/1.154m，面高.196m，每排双面宽.396m、盒外宽.34m，盒深.36m、行程.30m；中隔板随之贯通六抽段。暗拉口用斜切截面实体替代外凸把手，未模拟具体五金。连续横带由同色抽面/背板/转角饰面组成，转角饰面置x4.15–4.158避免占用抽面，加入门抽扫掠检查。柜顶承载/长门变形/拉口及安全登高均待实物深化。无外部操作，按本轮差异回退。

## 2026-09-09 split-drawers-46（历史）

三抽维持数量，宽抽base=.754/h=.232，两个小抽base=.99/h=.156；小面板宽.396、盒外宽.34，大面板宽.796、盒外宽.69。新增局部中隔板承接上排两抽五金，隔板和层板为结构占位，承载待柜厂。盒深.36、行程.30保留。抽屉侧板高度和分隔盒随尺寸自适应，避免旧分隔盒越界；accent仅一抽，所有门恢复暖白。2D文字按各抽中心定位；增加抽屉间扫掠交叉检查。无外部操作，局部差异回退。

## 2026-09-09 vanity-drawers-45（历史）

spec.sections/bays/doors重排为x1.95–2.65长衣、2.65–3.35双短衣、3.35–4.15外抽叠放。抽盒r=[3.405,2.80,4.095,3.16]、拉出.30m，三抽基高.752/.952/1.152m，高.196m，拉手约91/111/131cm。connection记录75cm横线与同色饰面，新增同高连接板，与原75cm妆台衔接；60/45cm深度进退保留，不虚构整块同深桌面。立面、平面、3D与交互选项同步。无外部接口/施工操作，按本轮局部差异回退。

## 2026-09-09 external-drawers-44（历史）

spec将入口80cm原两扇长门分成上下两对短门，中部不设门；三抽基高.652/.892/1.132m，高.236m，独立front与柜门共面，抽盒宽69cm。抽屉状态不再开门，drawer0/drawer1/drawer分别独立拉出40cm；短门把手/铰链随门高定位。叠放层移至外抽上方。尺寸立面、3D、文案同源更新。抽屉盒体和面板按全行程扫掠矩形核对其他家具和关闭的储物门；实际五金/承载/人体工学未验收，无外部操作，局部差异可回退。

## 2026-09-09 hinged-storage-43（历史）

新增master-foot-wardrobe.js，柜体/门扇/抽屉由spec.design.footCabinet同源生成，2D门扇轮廓与3D共用铰轴变换。六列名义门宽40/40/30/30/40/40cm；上门独立，顶部门上沿2.425m，低于幕布盒底2.46m。顶部固定封板只封正面，不侵占被褥格内部；模型净深55.7cm、顶格内高47.2cm，前口高度约40cm。

新增内部尺寸立面footElevation及closed/storage/long/short/quilt/drawer/all/inside状态；inside是剖视，不是物理开门。抽屉态只抽出最上层40cm。非关闭状态隐藏正常通路并提示开门占道；幕布下降时页面将柜门关闭并锁定选择，这是模拟交互而非实物联锁。

门体扫掠以1°步进检查房间边界、其他家具及幕布盒/空调实体；抽屉校验最终拉出位置与已开启柜门。尚未覆盖所有门间组合、凸出把手、真实铰链及全程抽屉扫掠，不外推安装通过。无外部接口或施工变更，回退只撤销本轮局部文件差异。

## 2026-09-09 desk120-ac95-42（历史）

梳妆左界和衣柜右界同为4.15；柜门/分隔/膝部尺寸同步恢复。AC x=4.275..5.225，机深高不变，送风指示同步；保留既有检修侧余量则范围4.155..5.355，右侧有5mm名义越界，仍保持安装未验证，不把恢复尺寸当作施工通过。无外部变更，按本轮差异回退。

## 2026-09-09 ac1000-41（历史）

AC r.x=4.275..5.275，其他高/深不变；送风指示随机身更新。service保留原左右120/130mm占位余量，变为4.155..5.405，与高柜/右墙分别冲突45/55mm。这是未选型检修候选冲突，不是机身实体相交：新增serviceWarnings展示并始终installationVerified=false；实体碰撞仍为阻断错误。防止为了显示模型而隐瞒冲突或把较小包络冒充检修通过。电源候选移到机下墙面，不虚构管路。无外部接口/设备控制/发布；回退按本轮局部差异。

## 2026-09-09 vanity115-40（历史）

39版未交付，本版承接：原展示端改15cm四分之一圆弧封闭收口，直柜扩至x1.95..4.20，台位x4.20..5.35。轨道/柜门/内部两格同步，双移门各约114.5cm、行程107.5cm；左格69cm示意箱移到分格内。桌下膝部左界4.23，净宽75cm，薄抽左界同源，不留穿柜抽面。柜背、床尾净距和原门/床/空调不变；AC机身距高柜侧12cm/右墙18cm为模型值，检修包络与柜侧相接，没有施工容差。床头取消织纹/分缝叠块，前圆角35mm、10mm下挂灯边；顶高不变。投影机底236cm、机顶248cm，黑上边候选10cm；安全绳随支架缩短，未声称光学安装通过。无依赖/API/外部系统变更；撤回只逆向本轮模型文件，不覆盖历史资料。

## 2026-09-09 woven-headwall-38（历史）

spec.head新增216cm宽织纹饰面和17mm灯槽包络，主渲染用确定性纹理生成薄饰面；压沿拆为四块围住真实槽口，不以重叠实体假装开槽。模拟光组仅夜间开启，观影关闭。未移动家具和设备，没有接口/依赖/外部系统变更；未改变卧室无吊顶条件。灯槽非施工节点，驱动不预设封在4cm背景内，须可维护。回退按本轮差异还原37版。

## 2026-09-09 mobile-bedside-37（历史）

单一spec移除nightstand、rightTable改成freestanding并更新40×40cm占地。主渲染以独立柜身、双抽面和四脚代替墙支架；details移除两组床头壁灯，主渲染移除弧线。门窗/大床/主柜/设备不变。无依赖或外部接口变更；可按本轮差异恢复36版，但不覆盖其他修改。

## 2026-09-09 headwall-36（历史）

仅修改主卧spec、模型装饰和尺寸标注。床尾柜前沿z=2.769、背沿3.369，两扇移门同步后移5cm；转角多边形以柜背为固定点收浅并复用到2D/3D。推拉轨8cm、背板1.8cm不缩减，净深50.2cm。半墙基高0、板高1.082、18mm压沿到1.1m，总厚4cm；床仍z=.05不移动。背景涂绘不作为实物画框；床头抽屉悬浮，基层固定仍待柜厂确认。无接口/依赖/外部系统变更；回退本轮受控文件可恢复35版。

## 2026-09-09 soft-layer-35（历史）
样式从34版延续。暖灰用在原高柜侧板、台面和背板，直接换侧板材质，不叠重面。镜格总高止于180cm，镜片基高96cm、高82cm、圆角18cm；开放格下设闭柜，镜门原33cm行程保留。幕盒安装包络不变，外形改小圆角方正收边；七个低强度宽配光模拟连续洗光，避免三块强光斑。仅为视觉模拟，不作为灯具配光设计。未改变落地家具、墙门及设备安装条件。

## 2026-09-09 vanity-wall-ac-34（历史）
仅改空调及对应视图、文案和检查。AC wall=foot-wall，r=[4.32,3.13,5.17,3.38]，机身长边沿x轴，出风面朝-z；base210cm、height30cm、top gap20cm仍为暂排。镜格顶180cm，与机底名义间隔30cm。检修区在镜格前上方；新增镜格/检修避碰和直送矩形包络障碍检查（非气流验证）。真实孔位、自然排水与安装净距未确认，不画施工线路。

## 2026-09-09 window-ac-vanity-33（历史）
同一 master-suite-plumbing-spec.json 驱动平面、家具、设备和审计。AC占位x=5.10～5.35、z=2.38～3.23，安装墙为vanity-wall（窗户右侧墙）；机底210cm、顶240cm、层高260cm均暂排。机身长边沿z轴，出风面朝-x，不再朝-z。
衣帽柜[0.02,2.12,0.67,4.62]；梳妆[4.15,2.919,5.35,3.369]；凳子/侧抽向-z拉出。镜格止于180cm、镜门沿x滑移；不加机下高柜。去除旧入口梳妆和85cm矮柜，去除卧室弧形吊顶。保留原结构。
新增check-window-ac-vanity.cjs防止机位转错墙、旧柜重叠、衣柜第三扇门和妆凳移动轴回归。仅修改本地模型，不涉及任何业务服务、外部部署、数据库或智能设备。
重要边界：挂机直送仍可能撞高柜侧板；图中偏转线只表达意图，几何无重叠不等于气流或安装通过。没有虚构冷媒/冷凝水线路。

## 2026-09-08 180cm主柜与连续圆弧桌

layout.wardrobe右界扩到2.88；counter、southUpper起点同步移到2.88，避免上柜与主柜重复叠放。cornerRadius=.06，cornerReserve保守包围圆弧添加部分并纳入通行校核。主柜分隔、两档挂衣杆、长衣杆、叠衣层板及被褥盒均显示；下部门板移门行程改86cm，上段柜门独立。新增interior显示模式只隐藏柜门，不隐藏柜体或家具。

## 2026-09-08 完整窗墙45cm桌段

单一layout将桌前沿从x2.83移至3.03，桌椅及电脑设备同步移20cm；连衣柜的桌板转角段随之加宽。northBooks止于z0.32，原窗洞起z0.34；南书架起z2.00，窗洞止z1.95；windowHeader底2.30m高于模型玻璃顶2.25m。上方横带仅为装修收口示意，不作为经核实的承重吊柜。模型与平面均引用新增书架矩形。

## 2026-09-08 一体转角柜桌

door-shift-layout.js新增joinery统一矩形；2D、3D及通道检查均引用。连续桌板以同一个Shape增加转角段，不叠放两块相交桌板；柜桌相接边分别与衣柜右边和桌前边一致。上部一组L形书柜：侧墙闭门上柜、下部开放书架、窗边实墙开放回转段，避免进入窗洞范围。校核将转角台面也纳入地面障碍，不能因悬空而虚构可走空间。

## 2026-09-08 独立改门试排实现

入口child-door-shift-study.html；child-door-shift-study.js渲染条件2D与旋转3D；door-shift-layout.js从原room导出坐标平移并统一提供家具、旧/新门和校核。新门洞投影仍接原living内走廊，原plan-spec.json和全屋GLB不修改。边界保持3.50×2.71m及原飘窗形状；新门洞在条件模型单独开口，不将原门洞残留在新模型内。

新方案首屏以模型为主，提供尺寸平面、全景、俯视、进门、书桌视角和门、移门、椅后拉15cm控制。53cm柜前警告一直保留。椅后拉30cm明确冲床，不提供无提示的30cm控制。不增加依赖、远程接口或外部发布；回退只需返回原预览入口。

## 2026-09-08 黑白材质及全房入口

fixedwindow.theme=monochrome；applyJoineryPalette集中配置白/灰/黑家具材质，关闭wood.map与normalMap并缓存原贴图，切换历史方案时恢复，不修改全屋GLB贴图。桌板增加同轮廓7mm黑色细收边，床底8mm黑色缝线与床头细分缝，窗台低书架及床头浅柜开放格背板为哑黑。床、衣柜既有真实体积保留，新增装饰不承担结构支撑。

roomstyle为本方案默认全房入口，“整体黑白”按钮显示所有家具，节点视角单独保留。HTML/entry/renderer/data缓存链同步为monochrome-full-room-0908。其他房间和其他候选的几何未改，无外部发布。

## 2026-09-08 连续桌窗节点实现

fixedwindow新增bayJoin：延伸投影[17.50,4.82,18.11,6.39]，位于原窗洞内；bayDesktop用一个凹多边形挤出台面连接室内前段与窗台上方后段，前外角半径60mm，台面标高725–750mm。原窗台实体继续保留，找平支撑从既有模型面层顶565mm至725mm，不创建空腿位或假柜门。两端低书架落在台面上，中央留空103cm；不固定到玻璃或窗框，书架高度与窗扇开启尚待核验。

显示器中心对准窗洞中心z=5.605。废弃南端椅子不显示，其历史路径不用于证明当前办公位可达。baydetail新视角明确隐藏床柜、只展示节点；其他视角恢复床柜，显示期望坐席红框，平面不再画旧椅位和旧可达箭头。新增windowSeatAudit单独记录与床的冲突，保留失败而非绕过审计。缓存链为retained-bay-desktop-0908。

## 2026-09-08 北墙床头及连续悬空桌实现

在现有fixedwindow入口覆盖被否定的南墙方向：床框[15.26,4.48,16.80,6.54]、head=north；窗桌[16.85,4.48,17.50,7.15]，单一267×65cm桌板。chair移至南端[16.18,6.56,16.78,7.14]，沿x后拉30cm或收进35cm。床头背景用单个L形挤出网格，北墙厚18mm、转接东墙实墙至z4.78，避开z4.80–6.41窗洞。浅收纳来自headStorage，不再使用windowBookcase。

预留两条隐藏跨度钢梁及两端墙支撑示意，无落地桌腿；钢材规格及锚固不是工程计算结果。修改相机看向北侧床头，二维显示关门后绕行箭头，侧栏说明门全开受阻。缓存版本north-flow-floating-0908，HTML、entry、renderer、data引用同步。其他方案与全屋GLB不更动；没有服务端/API改动。

## 2026-09-08 fixedwindow与成长型展示实现

新增固定床候选fixedwindow；studyfold翻床从可选数据撤出。床框[15.26,5.11,16.80,7.17]，衣柜[14.04,4.48,14.64,5.53]，桌[16.82,4.48,17.50,7.15]；窗旁书架[17.28,6.45,17.50,7.13]，在窗洞z=4.80–6.41之外的实墙。四层可调层板标高1.10/1.43/1.77/2.10m；浅书架只适合选定深度的书及轻物，不宣称可容纳所有画册或模型。

fixedWindowDesk使用真实桌架、显示器、键鼠与桌面电源盖示意，增加连续床头木饰条、侧相框层板及浅固定画框。模型椅占地60×58cm，五爪半径按占地计算；座椅、窗扇、插座及固定支撑待选型复尺。二维与三维引用同一候选数据；脚本缓存版本fixed-window-hobbies-0908。原plan-spec.json及全屋GLB不修改，本房局部试排未同步为全屋定稿。

## 2026-09-08 前期书房实现

studyfirst.bed=null，wardrobes为空；desk=[16.80,4.48,17.50,7.15]、front=west、桌面0.725–0.75m；分段0.72＋1.20＋0.75m仅作可拆连接设想。studyDesk生成桌架、移动抽屉、主机、显示器、键鼠、台灯、线槽及桌面电源盖示意，另生成独立低书柜。原飘窗保持实体，不增玻璃或改变窗洞。chair以70×70cm外包范围构建带扶手五爪椅，使用/收起/后拉45cm状态来自相同数据。

bed/drawPlan/尺寸标签支持无床状态，studyStorage纳入同一占地审计；界面改为书房标题，隐藏无关衣柜和床抽屉控制。长桌全景与桌面近看使用新相机。脚本缓存版本study-first-0908，原GLB不改；旧baywardrobe链接触发已淘汰提示，不回退为另一方案。后期改儿童房只是家具可拆装的预留，不声明床能直接摆进当前桌椅区。

## 2026-09-08 双柜与保留飘窗桌技术边界

新增baywardrobe数据，床矩形[14.96,4.48,16.46,6.52]，桌面[17.12,5.12,17.72,6.32]；desk.clearanceFootprint只记录室内占地，审计另校验完整台面位于房间/原飘窗投影联合内且高于实心窗台。未将飘窗加入人体可走区域，也未改变plan-spec或原GLB。

bayBridge桌不画独立腿、窗前吊柜或原窗台内的桌裙，仅保留待深化支撑示意。slidingPanels记录每组柜的移门，原两候选仍只有一组，新试排为两组；选择界面同步文字及默认收椅状态，允许切换使用/后拉以暴露路径限制。新方案初版椅背碰台面，最终床西移2cm、收椅位改为40cm位移后通过实体检查，未缩床垫、削薄椅背或削除原窗台。

缓存入口统一为bay-wardrobe-trial-0908。可通过选择southbedwall回看上一版；不撤销它、不恢复已删除的淘汰方案。未发布或修改外部服务。

## 2026-09-08 视频适配版实现

数据中增加desk.joinery：openBase=1.35、openTop=1.64、depth=0.28、displayWidth=0.34、cornerWidth=0.24（米）。styledBookcase按同一组数据生成120cm学习段与164cm置物段：桌面0.75m至书架下沿留60cm，开放格净高27.2cm；顶部六扇暖白门板，34cm竖向展示分三层；删除深棕四格。新增转角浅层板仅位于桌上，不增加落地侧板或独立腿，不改变移门及抽屉。底部被床挡住的164cm台段仍为不可用收纳示意，不画假抽屉。保留现有暖光示意，不重调全局曝光。

入口HTML、entry、renderer及数据引用统一使用video-fit-0908缓存版本，标题显示视频适配版与本房尺寸。原方案ID不变，其他候选不改。预览为本地文件，无发布、账号、API、依赖或外部系统变更；可依据此前文档与修改前内容恢复上一版立面。270cm柜顶及固定方式仍需现场核实。

## 2026-09-08 图片参考立面实现

southbedwall 使用 reference-horizontal。styledBookcase 沿原120cm桌段和164cm置物段分段生成、保持同标高：浅木背板0.75–1.948m；开放层板1.53/1.93m；顶柜1.93–2.70m，四扇闭合门板与左段局部四格深色展示。层板下新增四段扩散罩和四个局部暖色光源；仅是灯带效果示意，不是照度或电气设计。去除前版64/56/44cm碎分区与灰蓝点缀，原GLB和全局灯光不改。入口、渲染器、数据模块统一更新缓存版本。柜顶与安装高度为设计假设，承载与现场净高未验证。

## 2026-09-08 配色与非重复柜格试改

新增joineryStyle仅作用于southbedwall，通过styledBookcase区分左侧书区与右侧展示/书籍/闭柜。切换候选时applyJoineryPalette恢复旧配色或设置新配色，原GLB和灯光参数不变。新增少量几何摆件，不依赖图片生成。保留原连续台面、无独立桌腿形式及全部抽屉状态。参考视频未访问成功，当前试改不能视为视频复刻。

## 2026-09-08 转角台面简化

移除短暂试加的cabinetOrigin数据及其完整渲染/平面框体；改为legless控制删除独立前后桌腿，保留concealed-rear-ledger作为待深化支撑示意。台面和转角书架原占地不变，审计桌面恢复原矩形边界。近看入口改名“转角台面”，view=connection仍有效。未保留被纠正的包框方案入口，也未改其他候选。

## 2026-09-08 整排柜桌立面细化

southbedwall.desk.fullWall驱动wallBookcase，共用层板标高1.35/1.70/2.05m及柜顶2.70m假设。左书桌与右置物台面去接缝圆角，几何同高连续；整排六扇上柜门，下方两排开放书架。衣柜接角加上方L形返边层板（不落地、不占取衣通道），不更改衣柜105cm宽度。新增“柜桌正面”观察相机及view=joinery直达参数，明确不是步行视点。旧lwallstudy留作对照，新效果仍在southbedwall入口。

## 2026-09-08 整墙柜与抽屉状态

新增southbedwall数据，扩展south床头变换、储物床镂空框架、北墙固定台和吊柜、右侧抽屉柜、床侧两抽屉；不在打不开的床对应下柜画假活动门。新增收椅入桌下(-51cm)、使用位和后拉30cm，chairRect供SVG、渲染及审计共用；书桌抽屉拉出40cm、床抽屉45cm独立控制。上柜270cm高度标为假设。stowed chair仅椅座进入桌下，3D逐部件检查避免靠背穿桌板。

审计单独记录6种基础门/椅状态及床抽屉展开、椅后拉、全部抽屉展开且收椅的取物位。窗前不可达是明确未解决项，未把它写成已通过；旧方案仍执行原有窗侧要求。床抽屉取物位替代被展开抽屉占据的原床侧站位，不外推为全部活动自由。原plan-spec、GLB、其他房间及外网部署不改；撤回仅移除新候选及其渲染分支。

## 2026-09-07 L形柜桌实现

沿用data→2D/3D同源链路。新增integrated桌上柜分支：左端展示、右侧书架和上层闭柜，桌面无落地转角柱。衣柜移门复选框移动靠门端门板，拉手随面板移动；添加内部挂杆与衣物作取衣口示意。移门与桌面留20mm阴缝。SVG标明西门墙、北桌墙、指定取衣站位、63cm床尾和非通道桌端，新增进门/柜桌视点。旧cabinetwall移除，entry更新提示，原GLB与plan-spec不修改。无外部发布或接口依赖变更。

审计使用既有60cm代理与10cm门扇预留，不降低标准。最初取衣点z5.30距桌边27cm，代理与桌重叠；修正为靠门端实际站位z5.36、距桌边33cm，仍在约50cm取衣口范围。此修正不证明转角深处取物方便。回退仅删除新候选及专用分支，不复活用户已否定方案。

## 2026-09-07 一体柜墙试排实现

child-bay-options-data.js新增cabinetwall单一家具规格；child-bay-options.js补east朝向桌与椅、150×204cm薄边床、居中单枕、105cm书柜和连接收口，SVG及尺寸栏同源更新。书柜240cm高，下部封闭、中段书架、上段闭柜，与衣柜同顶线。entry保留精确ID门禁及旧方案删除提示，新增候选可选择，未发布外网。原plan-spec/GLB未改。新增仅为本地候选，不替换全屋方案。

审计增加书柜取物位、四状态主要路径、门扇10cm附加包络和实际枕头投影；110cm桌初试与预留包络冲突，因此缩到105cm，未降低校核阈值。材质沿用现有资源，没有接口、依赖、构建或外部服务变更。撤销本次仅需移除cabinetwall候选及其专用渲染分支；不恢复已否定方案。

## 2026-09-07 已否定方案退出活动预览

child-bay-options-data.js只保留windowdesk/windowstorage。新增child-bay-entry.js：有效方案才动态加载渲染器，失效/空scheme显示清理提示，不默认切到旧方案；渲染器choose取消旧ID重映射和默认回退。删除“原方案对照”链接，旧child-window-study.html换成静态移除提示。备份位于D:/tep-chat/backups/child-rejected-20260907-175855，包含原活动数据、页面、渲染器、审核结果及旧入口；不在网页发布目录中。旧独立JS等依赖未作无证据批量删除，原户型和GLB不变。

## 2026-09-07 180cm试排与门扇显示

沿用child-bay-options数据和页面，新增ward180不覆盖既有方案。门角度滑块0–90°同步SVG/3D；全开复选框及进门视点与滑块统一。门板36mm，试排门把手包络至门中心面70mm，模型明示假设非实测。审核0.25°角步进/5mm径向采样的80mm附加包络，并分门开/关、椅子常态/后拉30cm四状态审核；中间门角度只显示家具旋转，不能套用端点人体路径结果。原PDF门向重新视觉核对，与当前模型同向；未更改源PDF、GLB或plan-spec。

## 2026-09-07 沿墙重排实现

沿用单一options驱动2D/3D，新增wallbed，无screen，无飘窗拆除假设。隐藏被否定的privacy/crossbed按钮，旧scheme重定向wallbed；原数据保留为审核历史，原全屋与GLB不改。补书桌前支撑。门口相机正向对床尾段，床柜视点保留斜看情况，不能用相机朝向证明全面隐私。审查门开/关与椅子后拉状态的床侧、床尾、柜前和椅侧目标，新增断言防止几何改动后仍虚报路径。无接口或部署变更。

## 2026-09-07 隐私试排实现

新scheme=privacy保留旧方案。增加east床头变换、north桌柜变换、对应椅背方向与SVG床头标记。柜端screen从同一数据生成3D与2D，纳入碰撞/路径审核。进门视角自动开原门，不利用闭门遮住床头来冒充隐私。椅子控制按实际方案显示后拉10/30cm。新增门洞平面至头区的三维线段/柜体相交采样，屏蔽判断不依赖门扇；上柜及高柜须现场确认安装与防倾覆。

## 2026-09-07 飘窗四方案实现

child-bay-options-data.js 单一坐标源驱动新页SVG与Three.js家具；audit-child-bay-options.cjs检查主体与局部窗位的并集，未把整间房扩成更大矩形。原GLB与plan-spec不改，只在新页隐藏旧家具。提供四方案、平面/3D视点、原门开关、椅子后拉30cm、窗下抽屉及去顶显示。②允许座面局部伸入桌下，不能外推为其他家具可重叠。开门且后拉椅子、抽屉打开状态未独立审核，界面明确不套用其他状态结论。尚未完成浏览器视觉验收。

## 2026-09-07 床位比较实现与边界

三个scheme沿用查询键但含义已更新，用户可通过页面新名称区分。B bedReversed把床头、枕头和被褥转向对侧实墙；C床、床垫、柜、桌、椅、书柜从同一variants数据生成。C衣柜朝+z而非+x；桌面物件随新桌坐标平移。新增原门洞同铰点门扇的开/关控制，原GLB door-bed3-single只在候选页隐藏，2D/3D同步门状态。未移动门洞或修改源GLB。通行审核增加入房等待位置和关门后从该位置出发的第二阶段，不把关门状态当成可从门外穿入。对90°方案只作实墙条件下的几何排除，未伪造独立床头结构。

## 2026-09-07 三方案数据与预览

child-bay-study-data.js导出variants，以scheme=balanced/storage/study选择，只改候选家具。HTML方案下拉触发本地页面导航，尺寸、提示、3D及SVG使用同一方案数据；增加柜桌正面和飘窗视点。新增补柜下段开放避免未经验证的平开门侵入通道。audit-child-l-desk.cjs按参数分别审核三套，新增上柜不跨床、书托在原窗台内检查。原GLB和plan-spec未写回，无发布、外部数据及真实智能设备操作。回退使用原全屋入口。

## 2026-09-07 儿童房 L 形柜桌实现

child-bay-study-data.js 是本候选家具唯一坐标源，child-window-study.js 同时生成 2D 和 3D，HTML 说明同步。衣柜沿 x=14.04–14.64、z=4.48–5.58；书桌沿 x=14.64–15.94、z=4.48–5.03；床位 x=15.96–17.50、z=4.475–6.535。原窗台不计入可走地面。滑门沿 z 方向运动，椅子后拉 30cm，平面同步。audit-child-l-desk.cjs 检查家具重叠、门扇与可达性。无外部 API、数据服务、发布变更；原全屋入口是未同步候选的原版，不覆盖原 GLB。

## 全屋收尾阶段：同源连续行走（2026-09-06）

### 2026-09-07 · 具体摆法与路径校核

elder-layout-options-data.js记录6组床柜桌椅包络及床头方向；audit-elder-options.cjs从原plan-spec.json取不规则房间边界，检查家具重叠、20mm位置网格、91门扇角度及500/600mm圆代理的四邻域路径。目标是两床侧中部站位及空椅中心，不用仅到床脚代替床边到达。路径测试乐观使用整个模型多边形，不能证明窗侧实际可站立。数据与报告被elder-layout-options.js消费并生成平面和对比，不新增面积、不触碰墙门，不发布。旧elder-room-study默认状态改为转角路径警示并加比较入口，撤销其可能造成“有三边尺寸就是通行通过”的误导。无业务API或依赖变化。

### 2026-09-07 · 老人房独立局部预览

elder-room-study.html/js 只读载入 plan-spec.json 和 current-design-v2.glb。只在本页移动床及床头阅读组600mm、隐藏原580mm宽柜与窗边低柜，并添加真实600mm包络的1700mm推拉柜。study统一驱动柜体、尺寸图及桌椅冲突包络；不改墙门、原GLB、旧漫游或外网部署。柜门和边缘拉手同步平移；5种视角、原新对照、透明度、尺寸显示及冲突对照保持手机可用。回退直接访问原column-view.html。本页默认展示布局而非照片级效果；镜面为材质示意。普通书桌未解决，不能据此出施工图。

### 2026-09-07 · SH光照场局部试验

bake-living-probes.py以AST校验并复用render-current-design-v2.py的场景准备，仅排除最后的成图循环；同源模型/材质/镜面与窗光density35保留。8点×6方向、24px方形线性EXR、8samples，按像素立体角积分为Three兼容的9项实SH辐亮度系数，记录三种输入哈希。此为受保护的实验脚本，不接发布。

living-probe-pilot.js在客厅范围为1115个接收网格克隆材质/几何，额外写入probeDiffuse和probeWeight，不改POSITION。三线性场插值在顶点计算低频辐照度，保留原PBR贴图和直接光，局部替换均匀半球光/环境漫反射；边缘淡出，法线贴图细节仍走原材质。移动网格按world matrix变化重新取样；夜间uniform关闭；dispose恢复几何/材质/回调。静态场没有遮挡距离矩、全场景变更门禁或动态GI，因此不导入默认column-view.js。不是把旧失效烘焙重新启用。

### 2026-09-07 · 当前源 v2 渲染链

receive-render-source.cjs 是一次性本地开发接收器，仅绑定 127.0.0.1:8770；校验 Host、既有查看器 Origin、自定义请求头、固定端点、长度上限，固定新文件以 wx 排他写入。收齐相机和 GLB 后自动关闭，不加入网站入口或发布包。通过 CUA 当前页面调用已有 GLTFExporter，保留镜面导出/恢复流程；packGlb 逐 bufferView 校验无损合并。新文件 current-design-v2.glb 与 current-design-v2-cameras.json，旧 mobile-current 系列保持冻结。

render-current-design-v2.py 从原流程派生，改用 v2 源，取消布艺 Sheen Weight=.28/Sheen Roughness=.8 强制覆盖并记录实际导入值。暂保留旧窗口补光假设和相机，便于先判断材质差异。仅用于样片，不改变网页默认光照或旧图集。回退可继续使用旧入口，未覆盖历史渲染脚本。

### 2026-09-06 · 布艺底色与反光整改

fabric-calibration.js对已加载的两套CC0布料扫描做线性RGB均值去底色，生成可由glTF导出的CanvasTexture；中性反射率0.88并在材质色中补偿。粗糙度映射为0.86+0.14×原G通道；不改原图片文件、法线纹理或UV尺度。textile-details将表面roughness=1/specularIntensity=0.4/IOR1.45，柔光有效系数0.15；窗帘0.18。保留同一纹理共享逻辑，导入链统一版本号避免模块缓存混用。

当前vendor导出器只写sheenColor而遗漏Three的sheen标量，因此以sheen=1、sheenColor=原色×目标强度表达相同能量，使网页/标准glTF一致；未改vendor代码。既有冻结render-mobile-detail.py还强制把Sheen Weight设为0.28，未来新批渲染器必须去除此覆盖；本轮不改旧管线哈希或冻结图集。浏览器自检脚本仅用于开发检查，不接生产入口。


### 2026-09-06 · 沉浸预览与家具光照试验

viewer-focus-ui.js/css加入本房操作开关、帮助弹窗，桌面压缩主导航、次要工具进更多菜单；保留并复用所有原节点和事件。mobile-controls桌面动作也收起菜单，外部点击关闭；Escape返回菜单焦点。原主卧控制属于masterDressing而非bedroomActions，手机显式开放该面板；旧方案/指标移入折叠区，不删除原行为。column-view只接入sync，不改相机世界坐标或家具。改动模块带版本参数，防止本地实际捕获到的旧缓存监听器继续生效。build-site收录新CSS与JS依赖。

家具光照仍未接生产。furniture-vertex-lighting-pilot为被子增加可选120mm邻域、55mm高斯尺度和法线阈值的过滤，静态五网格试验；未经动态、夜间与当前全场景检验，不导入正式viewer。历史源和渲染图不覆写。


### 2026-09-06 · 电视墙横向一体化修订

当前入口tv-display-details.js替换四片旧饰面，保留原结构背板；新增整宽上下饰面，双侧550×1200×约198mm展示带。85寸屏幕及浅框前移100mm至局部z=-0.02，层板前沿齐平；原3300×380mm低柜不动。每侧两格、共四条遮光灯带，少量陶器与书籍，灰调木内衬。启动时检查屏幕尺寸、饰面包络、六本书及两组摆件落板与背板避让；失败抛错阻止无声显示错误方案。room-facilities/room-viewpoints/column-view.html同步描述。无接口、数据库或外部发布变动。旧高清图明确未同步本次电视墙，不覆写冻结渲染源。


### 当前接续：停步补帧与固定太阳阴影

试点烘焙：bake-surface-lightmaps保留旧默认批次和拒绝覆盖检查，增加显式单图集/采样/新输出名。单图试点写offline-render而非assets，因此不被当前发布装配收录；完整门禁仍检查五张齐备。新试点参数为atlas3/128/2048²，源仍ff101ba3…，不改冻结render-mobile-detail.py。隔离对照验证器在测试浏览器内拦截一张HDR及对应清单哈希，明确其余四张仍24采样；不会写回生产manifest或伪装为新的全屋光照。

column-view.frame在dirty早退前检查wasWalking到非moving的转换，设置dirty，使键盘keyup/摇杆归零后执行原完整composer；持续移动仍保留原轻量路径，不借此声称行走中完整画质。static-sun-shadow独立跟踪可见投影物、矩阵、position/normal/uv/index引用版本、材质透明/贴图变换与太阳/目标/阴影相机；首次或变化时刷新，其余复用。带蒙皮、形变或自定义深度材质的对象保守刷新。原镜面、室内灯光及试验窗口光照照常运行；shadowCache=off保留禁用对照。静态阴影守卫在实际渲染前运行，纯相机移动不改变世界阴影。模型与高清冻结输入无变化，待GPU绘制/像素及动态回归结果。

### 最新接续：固定光照检查减分配（2026-09-06）

surface-lightmaps保留初始整场景签名和逐HDR哈希门禁。运行检查只建一次3409网格快照，逐帧直接比较层级可见性、矩阵、属性引用/版本和材质，不重建网格/矩阵/材质数组；873个独立材质每轮只核对一次，颜色比较线性分量而非反复转换为sRGB整数。仍每帧执行，未靠节流掩盖家具移动后的过期光照。新增法线/UV属性替换、贴图偏移/旋转/版本、透明参数检查；回退保留外观编辑。profile-surface-lightmaps.cjs用于本机CPU对照及四种变更回退，不用于手机FPS认证。默认光照不变，回滚候选入口即可停用；高清源、相机、渲染器不变。

### 当前接续：固定墙面/地面光照快照

独立bake-surface-lightmaps.py复用冻结照片渲染器的场景照明前半段，仅更换独立UV源，保留其文件哈希，不改原脚本或照片。v2为59墙+1铺砖/5图集，日间、24采样、线性HDR及独立空场景OIDN；29994正在计算。surface-lightmaps运行入口仅daylight=surface-baked：整批不齐或签名不匹配则不套用；逐纹理SHA校验，全漫反射只计算一次，PBR镜面/粗糙度保留；家具/灯具/结构变化回退，纯相机行走不失效。默认未开启。测试须含着色器实际加载、UV/几何保持、移动后回退/恢复、昼夜和旧场景拒绝。

新增surface-lightmaps.js收集原匿名结构墙mesh_0..58和改名窗下墙，以及800x800-cut-porcelain；通过第二UV按16墙一图集打包，独立全屋地面UV。不只靠共享油漆材质识别，避免把同材质家具纳入。prepare-surface-lightmaps.cjs独立导出surface-lighting-v2.glb与接收面/当前遮挡物签名；原实时网格positions/normals/index不变，高清mobile-current源、相机和render-mobile-detail.py保持冻结。首个v1选择范围过宽，保留但不烘焙/接入。

### 当前接续：启动模型无损减重

新增build-site.mjs及npm build/build:check：esbuild仅做ESM编译与运行依赖追踪，发布仍复制现有模块路径；查询参数从磁盘路径剥离。显式收集页面/CSS、资产、五批图库及历史留档图；过滤不被运行时使用的EXR中间件，图库仅保留前端使用字段，不输出完整本地导出元数据。校验本批三份来源哈希、PNG实际尺寸、链接资源及保守25MiB单资产预算。--check不写文件；实际build拒绝覆盖已有dist或在12图未齐时生成目录。verify-build-preview以同一文件映射启动短生命周期本地HTTP，验证页面不依赖源目录额外文件；不是部署。

复用pack-glb-images.cjs对home-facilities.glb生成home-facilities-packed.glb；column-view.js仅替换加载URL及内容哈希查询，未改变几何细化顺序或模型源。27820844→14811056字节，5393个bufferView逐字节校验；启动贴图18→15去重，不重采样。原GLB保留；回滚只恢复加载URL。verify-starter-packing对原/压缩两次完整页面初始化后的所有网格属性、变换、材质及贴图尺寸比较。高清渲染mobile-current.glb/相机文件/渲染脚本不变，既有46484进程继续。

### 新批全屋高清与历史分离

render-gallery新增fullBatch选择：latest读取whole-current-hd-v1，legacy仍读取原full-daylight-v2。复用现有12房目录、刷新票据、加载错误清空、缩放与回3D/漫步；历史主卧/洗切/分方向与留档不合并、不改写来源。新批默认不替换旧默认，待12张同源/完整尺寸与视口验收再切默认；更多提供“新批全屋预览”和“旧全屋校样”。部分图片未生成时显示待生成，不拿历史图补位。新增verify-latest-gallery及--complete门禁分别用于真实已有图片和整批完成检查。

### 厨房闭合面与抓手槽修订

全屋取景修订：三卧/两卫/洗烘/清洁柜targets携带renderAspect=1与24mm direct-look元数据，渲染继续读取同一份相机导出，不改地面与墙体。两卫初始站位/方向改为现有已验证站点。连续路线审计按plan.doorLeaves仅操作房门，避免把所有橱柜/设备门的turn字段误当房门；当前8目的地连续往返已通过。旧历史批次的源与相机不重写。

补充快捷机位：column-view的cabinet/care复用room-viewpoints现有已验证的dining-sideboard/balcony-care坐标，避免旧快捷入口落入摆家具后的占位。其他家具与原图不动，明确的沙发坐姿tvSeat不改成站姿。verify-overview-targets验证22点及两修正入口的PC/手机同位进入行走；拍摄构图另靠当前批实际图像审查，代理点不碰撞不能证明整房入镜。

kitchen-details保留左门X铰轴-.511，双主门各519.5mm形成3mm中缝；盲角固定面终点-.514，新增29mm分隔封边和12mm右端封边，与门边留3mm。18mm门厚、模型前表面Z=.325的旋转轴，使开门包络不扫入同平面固定条，未声称已选实物铰链。门顶838mm；台面底860mm，金属上翼板底858mm，留20mm内凹抓手开口；删除无实体抓握依据的旧竖黑片。柜体分隔板上部前60mm开槽容纳连续轨道，后部延至台面底提供接触支撑。水槽/下水、三门七抽/台面足迹未改，无接口/依赖/发布变化。验证不过则按具体碰撞修正本候选，不回退其他房间。

### 公卫取消隔断 / 电视双侧浅展示

验收补充：通过限定bath1玻璃门前缀识别残留障碍，台盆以可见实体包围盒而非原点父组验证外置位置；桌面和手机分别操作各自定位容器，手机先展开更多再进入行走。公卫花洒局部视角改名“开放淋浴与花洒”，不暗示同一画面已含蹲便。旧洗切照片说明明确为固定版本，aside与最新展示柜/开放公卫要求同步。该轮无新增API/依赖或外网发布。

public-bath-details在现有深化后隐藏门、侧玻璃、两框柱，保留所有墙门窗/蹲便/外置盆与排水；移动新花洒分组父级到原淋浴根并保持世界位置，供定位使用。连续漫步已有可见性筛选，不应保留隐形玻璃障碍。column-view移除公卫淋浴门按钮，room-facilities移除对应入口和旧说明。tv-display-details只替换两块原侧饰面，新增同低柜宽的浅展示列，不动电视/沙发/结构墙；照明接现有accent场景。旧离线图保留版本，不冒充包含本轮修改。厨房缩门缝候选83141在87°碰侧向抽屉门板，已精确撤回本轮该候选，不留下失败几何；后续另按铰链与转角重做。

### 接续：有厚度圆角台盆与可移洗切用品

展示补充：render-mobile-detail新增可选day-task，沿用原日光窗源与GLB灯具元数据，默认day不变；room-viewpoints的ensuite-basin只加renderAspect=1。render-gallery在既有study机制下独立保存本轮/历史主卧两份清单，snapshot ticket防旧批异步回包覆盖新批；mobile-navigation添加photo=ensuite-basin/kitchen-work/master-bedding三深链。高清生成后再执行同模型/相机/管线哈希与PC/手机回到同位漫步验证。

master-bath-details保留柜根与双抽，盆口由同一圆角轮廓驱动台面开孔及多圈内/外盆壳，6mm外壳与真实下水孔；龙头补底座、阀体、手柄和定向起泡器，台面右侧双牙刷杯。kitchen-details在原台面根下加入可移用品组，沿用已有木材/金属/陶瓷材料。无API、第三方依赖、结构尺寸或现场水电改动；几何改变后重新导出，旧照片保持独立来源。回退为移除此节新增组及恢复旧盆体代码，不覆盖原户型/用户改动。验证扩展现有主卫和厨房专项，防止空腔被封、法线反向、抽屉穿盆以及台面物件越界。

### 接续：实际床垫取样与分离的被芯曲面

room-viewpoints新增master-bedding，沿现有目录接PC/手机细节按钮；verify-master-circulation添加beddingPhoto真实目标，不只检查相机所在点无障碍。verify-room-viewpoints校验目录/三视口，并成功后写同源相机清单。模型30ae975d…不因此变化，旧图库仍为窗帘修正前后冻结批次，不需覆盖它们。

textile-details/addBedding显式接收床垫mesh，bedroom-revision与主卧入口传入原对象。构造一个不进入场景的共享几何探测mesh，转换到床品父坐标，缓存竖直支撑射线；底层至少留1.5mm，避免原sideFall穿进床垫。两侧从床垫半宽内38mm处以44mm半径转向，再下垂120mm，主体法向最小6mm厚，织物UV按展开弧长而非投影宽度；80×40网格在弯折区加密。边沿闭合连接真实内外层，翻口随法向弯曲。所有探测都在初次装配执行，交互不重复拟合，仍非布料动力学。无第三方依赖/API/水电或外部发布变更；原图库仍为冻结窗帘批次，后续材质/几何改动不能混称同版。

专项audit-bedding-contact扩展到每床3321顶点＋6400三角中心，--verify断言无超过1mm穿入且多数受支撑采样在3mm内；原织物厚度测试改沿法向两层的欧氏距离，并要求竖直侧边内外分层。不得只看Y差为0就误判竖边没有厚度，也不得删除原床架/床箱/沙发通道门禁。

### 接续：同版双图而非单张校样

render-gallery的study清单指向curtain-room-v1，按固定ID顺序展示master-storage/master-bed，以各自机位名称标注；openStudy(id)提供原相机对应深链。mobile-navigation白名单接受这两个photo值。原master-square-final图加到历史留档，禁用历史图漫步，仍可回房间当前3D；原12全屋/5方向历史批次保持。verify-master-photo增加两个真实PNG及比例、前后/下拉切换、两机位步行、wrapper深链和旧方幅留存检查，继续检验新批次来源与当前GLB一致。无几何、依赖、API或外部发布改动；回退图库清单及深链即可，不删除已生成图片。

### 接续：全屏照片与可收起的次要操作

curtain-details原按展开布长190mm划分周期，主卧每叶14褶压在280mm收拢宽度内。现主卧8褶、儿童5褶、客厅12褶，老人13褶保留；每周期20段，横截面继续二分求幅维持实际布长；中段轻微不等幅、头部零交叉挂点不变。主卧/儿童根组x减30mm，窗台/家具不动。首版全屋粗褶出现249条姿态/对象接触记录，已按真实边界修正，不豁免窗台。verify-curtain-details挂点索引改用nx/folds，仍对每一挂点接触断言；21开合状态及当前主卧600mm通路/植物联合验证。

render-gallery保留原四批次选择和所有操作ID，把刷新/新图/历史切换与长说明移入原生details菜单；短版本提示常驻。单图禁用前后箭头，计数不重复房间名；菜单选择后收起、点外部收起、Escape优先关闭菜单再关闭图库。mobile-view.css使用剩余高度布局，手机竖屏全屏、横屏230px侧控，图片由既有ResizeObserver按真实比率适配，仍有1～4倍缩放/捏合/拖动。无依赖、外部API、存储或模型改动。两个现有图库验证脚本按真实用户路径先打开更多再点击，增加手机图片实际显示尺寸和Escape门禁断言。回退仅撤对应图库模板/事件与样式，不恢复旧几何或覆盖照片。

### 接续：按真实可站区域选择摄影画幅

audit-master-framing在原主卧低位网格障碍下枚举155个600mm站位，比较24/22/20mm、16:9/4:3/1:1及两种眼高/瞄准高度；九个柜门样点进行投影和遮挡射线。24mm方幅选[17.8,1.6,1.6]看[15.88,1.3,3.652]，最大归一化边界0.9263，九点可见；网格内宽屏无合格点不等于不存在其他连续位置。六状态600mm通路新增cabinetPhoto终点证明入口可达，不把仅站点free当连通。

room-viewpoints为该点标renderAspect=1；render-mobile-detail按每视图比率设置高度及传感器长边方向，默认16:9保持。3D仍按设备窗口显示，可环顾，不拉伸照片或房间。render-gallery增加独立“主卧柜墙新图”固定批次入口，加载master-square-final清单；不与历史12全屋/5方向混版，错误清空本批不回退旧图，回3D/漫步定位当前命名机位。照片不是实时动态门状态的截图，明确固定版本。无依赖/外部API变化；保留源模型和旧图，回滚局限于该机位/画幅分支及图库按钮。

### 接续：主卧可见物体清障与布局重适配

取景补充：wrapper直接以layout打开时首帧仅留下发灰合成结果，DOM无遮罩、相机不变，重绘第二帧恢复；启动阶段现在保留一次阴影更新/重绘后再按需停帧（不是持续满帧循环）。92622初始两帧无需点击已显示正常。初版高清master-storage从入口斜拍被柜侧板占据，现同一24mm机位改[14.65,1.60,1.85]看向[15.88,1.30,3.90]，不剖墙/不挪柜；74192验证可站位和PC手机23方向入口，输出相机SHA256=43286b581739816349898132db30d7f2ea4c3eb6daf75ea72d275282701f8248。第一帧内部渲染原因尚未细分到具体后处理pass，不把症状修正说成驱动根因已确认。

master-dressing按已核实的花盆中心15.09/3.32m与260×350mm尺寸定位唯一29网格植物，原样移到17.70/1.10m、底接6mm完成面，并命名master-window-plant；分组或尺寸变动则报错，不猜新对象。此修正位于共享viewer装配，不依赖重建旧app资产，切换主卧方案不会复制植物。lighting-design仅主卧改300×12×10mm床底灯槽候选，灯口随床架重建；支架、驱动和接线仍需实物设计。column-view的ResizeObserver在layout模式重算现有米制边界取景；不改室内拍摄视角和几何。

新专项按30～1900mm高度范围全部可见网格AABB检查600mm圆形代理，32周向边界探针、25mm网格、路径每5mm复核；旧盆栽位置回放必须复现右床侧不连通。包含梳妆占位与滑门两状态，但不是完整人体、两人或现场施工验证。无依赖/API/远端变更；回滚只撤本节对应函数/挂点/ResizeObserver重适配，保留历史证据，不覆盖之前柜墙修改。

### 接续：餐边柜—设备区联合修改

收口补充：设备区四周竖板底边改与12mm底板上表面对接，去除原重叠/重合底面；原户型与外部包络不变。continuous-plaster-finish只给该连续抹灰表面，离线渲染不再对每块拼板添加独立着色倒角，柜/金属/其他材质不随意重写。旧整屋图库标题和说明明确为历史批次，新v2柜墙校样独立保存，不把不同来源合并声称同一最终版。

hvac-details保留局部设备/送回风组件坐标，以root.z=.6整体后移；hvacDesign输出世界坐标，设备区x7.48～11.38/z6.02～6.96，底2.43m。后沿退至双眼皮内层前，未切原吊顶/墙体；450mm检修孔前移100mm避柜门。sideboard-details逐块重建高度而非缩放分组：18mm顶/侧板、20mm门板与深宽保持，上柜顶2.41m/门顶2.406m，保留25mm下延；移除最高层板和6低频盒，改两层杯具/90°平开，移除原未选定的同平面滑移示意。末段切换动作在column-view/hvac-controls互锁：检修前关柜，开上柜前收回检修件；不能据此声称两者可同时使用。room-facilities同步说明取舍。上轮两墙烘焙来源已过期，baked-wall-study明确回退，需新同源烘焙才可重新启用。正常导出后再渲染同版校样；旧图库保留历史版本，未发布Sites。

### 接续：显式线性HDR降噪阶段

inspect_wall_bake.py直接检查原始HDR六面图集并输出诊断预览；denoise_wall_bake.py校对原图SHA，在Blender合成器执行HDR OpenImageDenoise，不把显示色彩空间图片模糊后冒充光照。原始v1保留，v2保留32位EXR中间值、HDR、来源哈希和能量/高频统计；受光像素平均能量变化大于15%直接失败。render-mobile-detail.py仅--bake-child-walls分支在烘焙之后自动接此阶段，普通高清渲染不受影响。baked-wall-study读取v2，仍受两墙顶点/世界矩阵、运行中几何变化及夜景回退约束；只通过daylight=baked-child启用，无新依赖、接口或外部发布。退出该参数回到原预览。当前尚无整屋启动状态/全部材质内容的烘焙指纹，家具源码变更必须重导出重烘焙，不能默认全屋推广。

### 接续：daylight-design实时试验

窗光试验截图出现硬边/块状阴影，未启用默认。后续另做daylight=baked-child两面灰泥墙试点：prepare-daylight-bake在已读回的mesh_55/mesh_56盒体上只加无重叠第二UV，完整原场景无损导出供Cycles计算直接+间接漫反射（不含反照率），输出线性HDR和原几何指纹。渲染器新增显式--bake-child-walls分支，不改普通渲染路径。浏览器只对指纹相符的两墙用BasicMaterial.lightMap显示全漫反射，避免原半球灯叠算；相机可转动，模型可见几何/变换改变或切夜则回退原材质。不是全屋烘焙或动态GI，未默认替换其他表面。RGBE/Three及Blender复用现有工具，无新依赖。可回退查询参数即恢复原预览。

独立模块从当前可见玻璃命名/世界边界与房间坐标推得向内法线；不创建新开口。固定天空代理在窗外，三个就近光源槽均有阴影，窗玻璃只在试验开启时不作不透明阴影，退出恢复原标志。缓存可见投影几何/变换，只有光源分配或模型变化才更新阴影，不因相机原地绘制重复重算；夜景关闭代理。column-view仅daylight=study路径接入，默认未改，原灯具8槽不动。减全局环境/半球泛光与日光源强度为美术曝光假设，非物理太阳/光度模拟。不改材质颜色/几何/碰撞或导出格式，离线Cycles不受该试验场景灯影响。

### 接续：餐边柜/冰箱当前模型深化

接续：fridge-details去除原mesh_766实心顶柜，保留两侧外柜/后板，补三块实际层板与4轻物盒；mesh_767挂到fridge-upper-door，setUpper(0..1)绕竖轴90°，reset联动归零，独立顶部收纳入口。sideboard-details把mesh_714改板式窄检修腔，mesh_715为独立检修门；面板厚22mm不动，侧板/顶帽前缘回退9mm避原6mm交叠。隔板分水电区、支撑件接阀体/电源盒；内侧壁留两个20mm过线/过管孔，线尾止于设备背前，live=false，不修改建筑墙或外部机电。原setService布尔兼容数值0..1用于90°检修门检查，不再移动管线机主体。共用PC/手机同一状态，不新增API/依赖。

碰撞整改：上柜改coplanarDoor两段平移（先Z+0.034，再X±0.650），底柜保持90°铰接；轨道/连接片为安装包络，未建完整悬挂传动。验证仅对“客厅双眼皮”复合网格追加三角面窄相检查，排除环形吊顶空心区的单OBB误报；不豁免风管机实体、柜体或设备。冰箱壳体深561mm/中心Z=-0.0225形成9mm门封退座，外门与后面不动；开角125°，铰轴改同心圆柱而非跟门转动的长矩形盖，5个篮保持原尺寸及300mm行程。PC/手机沿用同一个状态接口，新增整体按钮不关闭设备或抽篮；分格按钮仍切近看。无需依赖/API或外部服务变更。

pack-glb-images.cjs：只处理单内嵌缓冲GLB，SHA256对相同字节载荷和相同元数据的图片去重；保留所有bufferView索引及其原始字节，重排4字节对齐的BIN偏移，重映射texture.source与已知图片扩展source。遇到无法安全重定位的bufferView扩展则报错。逐项断言所有原bufferView内容、几何/节点/材质定义不变；不改分辨率/UV/采样器/色彩空间。export-current-scene完整导出后自动执行；camera-only不重写模型。新增脚本无依赖，回滚仅移除导出器打包调用，原始viewer仍消费其原基准资产。

新增sideboard-details与fridge-details两个职责模块，在共享viewer完成livingRevision后应用。只改当前GLB中已读回的命名家具子树，不挪墙、岛台或家具根坐标；保留原外露门板尺寸/材质与机位入口，抽屉面板和盒体用共同父组移动，柜门绕实体铰轴开合。冰箱保留原外柜与四门，替换实心机身为保温壳体及内仓；设备安装方式明确为待选型包络。管线机面板前移仅停机检查示意，背后接头不拉伸成假线路。新构件复用Three和现有浅木材质，不增依赖/API。控件变化触发原渲染/碰撞刷新。回滚只撤本次模块及界面接入，不改基准资产或外部Site。

### 接续：当前viewer洗烘细化

新增laundry-details模块在applyLivingRevision之后、灯光/碰撞初始化之前替换导入洗烘机的简化壳体及门；保留柜位、设备根节点名称/变换和原任务灯。复用Three、圆角盒、现有控件/设施入口，不增加依赖或外部接口。门扇使用独立铰链组，状态改变后标脏并由现有碰撞缓存识别；当前导出器读取相同模型。回退只需撤销模块接入及对应设施/操作代码，不改源户型或基准GLB。旧高清批次不自动视作更新。

门铰最终本轮候选改为local +X，即远离玻璃侧，正角0～110°；原local -X在71°起穿窗帘网格而否决。原显示/旋钮保留四部件，实心壳换板壳和真圆孔，内筒、提升筋、门封、玻璃凹盘及门框独立；内筒小孔为程序材质纹理，不是逐孔开槽。下机顶—弹性垫—支撑轨—上机垫之间具备接触关系，但没有原厂连接件/承重认定。机后仅有当前柜内几何余量，水电连接仍未设计成已接通。三个共享操作按钮、四个细节/站位入口随PC/手机同源更新。

洗烘门玻璃由alpha混合改同源物理透射0.94、IOR1.5/厚4mm；离线与实时共用材质。原24mm洗烘整体取景裁掉下机，保持24mm不改墙或放大房间，机位退到阳台通道[7.65,1.55,.74]，独立检查完整柜包围框八角在16:9画面及游戏代理站位。export-current-scene新增显式--cameras-only，仅在已有模型导出且只改机位时使用，保留并回报GLB哈希；不能用于几何/材质已改但未重新导出的情况。无外部发布或依赖变化。

### 接续：座椅保留的离座检查

纯函数seat-egress.js消费当前模型读回的家具/椅子包围框与原计划主横厅范围，用600mm圆形站位、25mm网格求至入户主通道的路线。活动椅不删除，其他五席保留使用占位；仅在椅侧后近邻采样站位，禁止用远处空地冒充起身位。原全拉出压力场景保留。前端与独立验证共用输入契约，但独立验证需沿输出路径复查实际碰撞。无墙门窗或API/数据变更，方案是否需挪家具以当前检查结果为准。

bar-swivel-seats替换四吧椅下部连接构件、增加可旋转座盘父级，保留软座/低靠背几何尺寸并接已有本地织物材质。living-seat-use的working/stand状态四吧椅原位，stand仅活动吧椅转90°；岛台椅仍按50/450mm平移，pulled状态仍全部450mm。站位图来自当前椅实体及其他五席的占位，路径按主横厅计划范围读取。column-view提供循环转座/归位和共享设施入口。旧audit-seat-egress脚本留作“原直拉方案”反例；不能把旧报告当成当前旋转版。无新依赖/网络素材，GLB需重新导出，旧PNG不因此变新。

补充8条随座盘转动的金属靠背承托；不把软靠背外形本身当作结构连接。新增export-current-scene.cjs复用已验证的GLTF导出步骤，直接读取当前column-view（旧export-render-scene仍默认index基准，不混用）；只在初始化与六镜面就绪后生成GLB及对应相机记录，避免为每次导出重跑无关整套UI测试。该工具不替代任何布局/视觉验证。

### 接续：浅木板件共享纹理

cabinet-finishes.js 提供显式板件材质替换及米制 UV；同步创建纹理对象、另暴露加载 Promise，使构造函数不依赖调用者提前等待；当前 viewer 仍等待贴图后显示。只克隆目标几何并改 UV/材质，不改位置、法线、索引或变换。落地延腿后刷新 UV，移动抽屉不刷新以免木纹滑动。三卧/梳妆/厨房/岛台按局部材质身份选板，不全场景按颜色猜对象。无接口、数据库或部署配置变化；未命名的旧玄关/餐边/洗烘板件暂不按颜色批量替换。

### 接续：窗帘布面与开合

download-render-assets增加可选资产ID筛选，本轮仅请求white_oak_veneer，不重新下载整包；保留每文件官方MD5校验，无参数仍执行全目录，未知ID拒绝。新材质资产仅预备，尚无模型调用，不改变本轮渲染GLB。

curtain-details按高度缓存横截面：开态底部比顶部散开80mm、全合时归零，每行独立求恒定弧长与UV；顶端相位为零使挂钩接布位置不在褶皱深处。挂点下延6mm接布，深6mm容纳在双轨内；帘布网格数量不增加。被芯上层增加不均匀厚度，内衬与边缘保持原高度。参数化加厚枕头校样仍像块体，最终三卧改复用本地CC0枕套网格：协方差主轴放平、按既有宽长/200mm厚适配，不改变床框或已经落座的沙发靠包；原床箱父级保持。诊断MSAA4没有消除原细碎亮线，未将其加入生产渲染，不据此增加手机负担。

render-mobile-detail增加第六个位置参数daylight_density，默认95保持原行为，有限值范围1..300；45仅为本轮窗源补光渲染试验，不是实际灯具瓦数/照度。manifest增加daylightDensity，续渲必须连同模型/相机/渲染器hash及光景一致，防止把不同设置混入一批。现场材质、设备和户型没有因此得到施工认证。

增加独立诊断脚本inspect-curtain-shading.cjs，仅在测试浏览器对当前column-view请求做AO对照、对帘布做纯色无光照/平面着色材料对照并输出截图；不改生产相机、几何、材料或全屋照明。目的为区分布面破孔与着色亮线，不能以诊断截图冒充设计效果。全屋细节审计顶端维护16类最新状态总表，后文明确为历史基线。

curtain-details在当前viewer加载GLB后替换四个原窗帘组，旧组改名隐藏留作对照，保留共享父级的顶面显示逻辑。按实际柜体包围盒推导湖景轨道端点；数值求解波幅使每叶横截布长在开合时不变，UV按累计弧长映射，同一实拍terlenka织纹复用textile-details导出函数。挂点随展开跨度移动，织物无非等比父级缩放。末端摊平区和5mm前后错层容纳闭合搭接，不以双层同面重叠代替厚度。curtain-controls提供共享PC/手机范围控件，不增加后端/真实设备操作；定位回原roomFacilities对象。可撤回viewer引用与新模块恢复旧组；原计划尺寸数据不改。

### 接续：家具地面接触

finish-details/finish-joints引用tileLayout.surfaceY，踢脚主体底=floor+2mm，高60mm；同斜接轮廓挤出2mm底部密封条，不再硬编码18/48mm。门套侧框由floor+2mm延至原2.20m顶部，底部独立薄封条，外侧细缝从floor开始。支持原重建/销毁流程；家具支撑后柜体低点变化会触发现有踢脚避柜扣减，分段数以当前运行态为准，不固定36段。回滚限于饰面模块及新增节点验证，不改原尺寸依据。

lighting-design.addFootlights不再因已有灯组就跳过定位。每次sync按当前床框边界重新挂接位置，复用原灯材质/灯口，不重复新增；修复候选clone继承旧床框安装点的问题。verify-lighting-design补换案后20mm安装偏移检查。原床恢复测试的baseline在灯具装入前取值，改对原家具逐子级包围盒对比，后装灯单独验证；不移除任何家具通路障碍，也不放宽通路结果。

新增furniture-floor-supports.js供bedroom-revision/master-dressing共用；从目标底板真实局部包围盒与tileLayout.surfaceY=0.006推导支撑高度。addGroundedPlinth构建18mm周边条与间距不大于约650mm的横撑、3mm底部弹性条，保持目标底板、床面和XZ轮廓；不使用贯穿床箱的实心填块。groundVerticalLeg保持原顶端，把已有竖腿/侧板向下延长到4mm脚垫上。支撑随固定床架/柜体，脚垫随座椅父级移动，不挂到上翻床板。主卧方案切换重建同样节点，材料/几何在各组件间独立，不破坏原候选销毁流程。无新依赖/后端/水电变更；可撤回两处调用与模块恢复原状态，原图和墙门窗不改。

### 接续：同源房间相机目录

room-viewpoints.js只记录room/id/name/position/look/subjects与镜头参数，不持有几何。column-view和mobile-navigation共用选择入口；当前目录独立于房间tour，不增加伪房间。相机采用direct-look、24mm/36mm长边，网页按实际aspect换算纵向视角，离线横幅相机读取同一参数，不再使用旧机位的自动移轴近似。两处模型导出器保留viewpoints字段；旧targets仍兼容，旧图库不冒充新图。新增angle查询参数直达取景。无需新依赖、无后端/API/数据库影响，撤回目录引用与选择入口即可回退，不改户型文件。

mobile-preview原固定iframe忽略外层查询，现仅透传space/angle/mode/v至同目录viewer；不允许更换iframe源。render-gallery以独立模式读取room-angles-v1，与旧full-daylight-v2及历史留档分开；返回时调用原房间+同一取景，可原地开始漫步。加载失败清空旧图，刷新序号防止过期请求污染当前模式。场景GLB保持9f4dbcb4…，只更新相机元数据；不因本轮校样而声明全屋同版终验或最终发布。

### 接续：织物材质与表面

扩展现有download-render-assets，以官方API/MD5取terlenka与cotton_jersey的1K diffuse/nor_gl/Rough；不改原有素材，保留CC0来源。textile-details按约0.266m/0.264m扫描块换算重复，颜色sRGB、法线/粗糙度线性，分配床品细织与沙发针织；现有网格局部变形增加收褶，不改变家具基准。PC/手机与GLB共用相同材质和几何，不用离线替身。撤回织物模块改动即可回到既有版本。

throw_pillows_01两网格在本地加载，原红橙材质不进入场景；等比0.85烘焙几何、补偿沙发父级X缩放，底部采样落至座面上1mm。座垫按实际X缩放算织纹重复，原扶手/靠背改米制UV，防止长物件拉长织纹。textilesReady与初始模型/HDR并行等待，避免导出或首帧贴图未就绪。detailDistance仅用于四个材质近看入口，沿原取景方向接近，标明非站姿；不改变房间或连续行走控制。

### 接续：主卫饰面排水

master-bath-floor在两卫旧模块完成后、installTileSurfaces之前执行；克隆带公卫两洞的Shape再加入主卫饰面洞，保留坐标和形状元数据。主坡、反坡和6mm门口带分片裁800砖，2mm缝及低0.8mm填缝跟坡；隐藏旧13个凸地漏部件，以1040×60mm槽/格栅替代（产品待选），格栅可提升80mm。主卫玻璃门增加胶条与滴水边，需复核全扫掠及局部路径。无新依赖或户型修改；回退新模块调用与相应入口即可，当前full-daylight-v2仍是此前导出快照，未包含此修改。

### 接续：全屋高清校样入口及碰撞刷新缓存

render-gallery.js读取full-daylight-v2-detail-renders.json已生成记录，固定12个合法房间ID，只接受本地PNG文件名；每次选择/关闭递增请求序列防止慢图覆盖新房间。缩放使用图像原比例，1～4倍；PC鼠标拖动、滚轮、键盘与触摸捏合共用容器滚动。旧mobile-*.png仅在“历史留档”中显示并隐藏“从这里漫步”；回3D使用当前房间而非历史家具。manifest读取失败、图像加载失败和未完成房间均不落回旧图。mobile-navigation复用原入口，desktop按钮改“全屋高清图”，移动端从全部空间进入；图片是导出时固定状态，不随实时柜门或灯光变化。

continuous-walk.js维护可见网格/门的世界矩阵、几何引用/属性版本及walkThrough/turn快照；350ms检查未变化则跳过原rebuild，变化时仍执行原包围盒与网格索引算法，未缩碰撞体或改路径。弹窗/输入焦点期间step不重建、不移动，原MutationObserver清键保留。撤回refresh缓存和图库模块即可回到旧行为；没有API、依赖或户型数据变动。

### 接续：窗源日景与餐具细化

render-mobile-detail.py读取导出窗玻璃边界及plan-spec空间内侧方向，补七空间实际窗面的室外面光源（95W/m²辐射功率仅艺术参数，不是电功率/照度）；隐藏的阳台侧窗不导出，不参与补光。光源位于玻璃外，保留窗框/窗帘/家具遮挡；两卫磨砂窗保留外观并按阴影射线衰减透光。保留原HDR映射/朝向，避免改变已确认湖景构图。清单记录各窗尺寸/坐标/房间与模型、脚本、机位哈希。

tableware-details.js使用标准可导出网格/材质：旋转体薄壁杯、圈足盘、独立液面、刀叉及双层餐巾、接水底盘/可取格栅、空心壶嘴；当前viewer加载后替换旧同名对象，保留设施入口及家具布局。16只抽屉内盘沿用同一盘形。撤除模块调用可恢复原导入物件；渲染脚本撤回窗源段可回退，不改户型和外部服务。没有新增设备采购或水电连接。

### 接续：同源铺砖表面

实际原点[x=0.175,z=0.400]m，模数0.8m；基于当前主横厅宽向净界5.92～12.03m，两端名义切片655mm。ShapeGeometry原有三角面逐个做矩形裁剪及门口区域扣除，合并一次提交而非211个独立砖对象。公卫tileCuts同步新网格，但排水高程函数不变。常规砖面物理高程保留，原底面下降0.8mm做填缝可见层，不改变结构楼板。PBR使用确定性低对比细砂色图、非色彩法线/粗糙度，导出为标准glTF贴图；不是新拍摄素材。tile-controls提供同源排版和检查机位；focusFacility新增可选look/fov，仅显式提供的细节项使用，不改变原房间机位。

tile-surfaces.js提供800mm模数/起铺原点与裁片函数，公卫坡面引用相同网格。根据现有floor.geometry三角面逐片裁到每砖有效区，保持原多边形/孔洞，不重新描户型；合并平铺砖网格与标准PBR材质、顶面高度不变，原面下移0.8mm作为填缝底。两卫门口仅平接分缝；无楼板改动。所有贴图为可导出的标准map/normal/roughness，避免实时自定义着色器与离线脱节。新增定位/排版复用当前页，不新建户型版本。原GLB保留可撤出模块回退，无外部依赖/API变更。

### 接续：空调细节

当前实际实现为hvac-details.js＋hvac-controls.js/css，在主卧/干区方案接入后、灯光与漫游初始化前执行。控制入口位于PC顶部、手机“更多”，四个区域切换；切走或俯视会收回检修状态，导出也强制reset。设备吊顶为带真孔薄板，侧送改设备位直接短接，不保留被否决的细长支管。12组开合各91采样；闭合挂机外廓940×300×217mm（含前罩/叶片），不把200mm壳深当总深。原GLB与隐藏旧组保留，撤除模块及入口可回退；没有外部API/设备连接。

hvac-details.js在当前GLB/卧室家具修正后实施：旧设备吊顶及风口保留隐藏基线，新结构用可见薄板和真实孔口组成；机身包络沿用services.js的1200×600×220mm。底部滤网和独立450mm检修口分别操作，停机检修为手工演示，不冒充已选成品机制。三卧单位坐标以原墙完成面为依据；机器尺寸、容量和外部路线未确认。共享column-view集成PC手机与同源导出；原index/app生成基线不覆盖。

### 接续：灯具注册表与照明预览

新增lighting-design.js：读取当前可见灯口及世界坐标、克隆发光材质保证独立控制，命名与导出元数据稳定；实时使用固定数量带阴影的灯池，按机位选附近照明，避免几十个阴影光源同时拖慢手机。该近似不等于全局光照，离线Cycles读取全部灯具参数。lighting-controls.js提供共用场景及逐灯定位。原日夜按钮保留兼容。布局候选更换后重新索引新增镜灯，不能保留已拆对象。原GLB不覆盖；模块撤出可回退。无外部系统、接口或依赖变更。

具体为38个注册灯口与8个1024px阴影池位置；摄像机移动只在附近光源集合变更时更新灯影，门柜等可见遮挡网格的矩阵或集合变化会失效缓存。该机制仍会因光源取舍出现照明近似，不是全灯具实时GI或游戏帧率认证。光通量转实时强度包含0.045美术曝光归一化，不可据此读实际照度。所有灯口元数据写入GLB extras；离线根据同一位置/方向/灯口尺寸和场景级别建光，3000–4000K、Ra≥90与瓦数均为选型目标。

离线试片采用24mm/36mm传感器的水平建筑机位，通过shift_y重新构图保留垂直线平行，不缩放房间。晚间使用1.5EV曝光设定；这种渲染摄像机设置不是实际拍摄参数验证。日间照旧保留湖景参考HDR与窗外补光，湖景不是13F实测。

### 接续：公卫细节模块

public-bath-details.js接入当前column-view原GLB后，在原浴室几何位置深化，保留旧对象为隐藏基线。窗的双轨运动在既有洞内；浴室门、蹲便位置不改。淋浴饰面槽通过同一outline重新生成地面孔，不以叠加斜面掩盖平地穿插；使用原800mm砖系列裁切，最低处仍在示意饰面层中，结构/排水连接未确定。拟用publicBathDetailsDebug控制淋浴、窗扇、浅柜、吊顶检修和复位；新增对象纳入连续行走、设施定位与导出复位。不改其他卧室家具与阳台布局。

### 接续：厨房实施入口

验证补充：verify-kitchen-details.cjs用可见网格与OBB分离轴检查抽屉/门扇扫掠，避免旋转门的AABB空角误报；净边界阶梯点2.50m采用1e-8m数值容差，非修改墙线来过测。检查服务仓对柜板、转角两柜和台面交叠；600mm局部路径采用20mm网格。默认厨房相机[5.33,1.6,5.02]看向[4.32,1.10,3.16]，非改变房间宽度。导出前新增kitchenDetailsDebug.reset()以防检修状态混入全屋基准GLB。

kitchen-details.js在column-view加载原GLB后替换两套原柜，旧网格移入独立隐藏baseline（不参与当前柜体边界/导出），保留原盆、灶和烟机对象及平面坐标。西柜三模块带完整壳体、七只400mm抽屉与45°手扣；北柜统一盲角、后背检修口、阀门管路示意及三个门扇。烟机下移250mm，延长罩壳保持原顶部，未创建建筑排烟连接。kitchenDetailsDebug提供setDrawer/setSinkFraction/reset；设施定位会打开对应抽屉或盆柜，PC/手机共享四个简洁动作按钮。待专项验证后记录实测结果；原材料/墙门窗不作施工级承诺。

### 接续：门套与踢脚线节点模块

finish-joints.js从designWalls的door类型生成六处门套组（只生成住宅内部侧），45mm宽、12mm外凸，卫浴采用暖灰铝合金示意。finish-details.js按同一门洞扣除门套宽度/2mm缝，柜后仍按实际柜体扣减。匹配近邻垂直墙段的中心及内外边线交点生成45°斜接，排除门套/柜体切断点与柜内交点；其余端头加封口。重复构建先清理自身旧节点，不动原门框/墙/家具。无新依赖、无API变更；原GLB保留。

### 下一项厨房：当前证据与待实施边界

当前运行态kitchen-cooking-cabinets位于[4.03,0,3.54]，前7个子网格是实心柜/台面/5扇门，另有kitchen-hob及kitchen-hood；水槽柜在[4.86,0,2.17]，现有真盆保留。灶柜与水槽左侧板转角相交。plan.js厨房净多边形：z1.90–2.50段左界x3.80；其下左界x3.70；右界x5.72。旧灶台面x3.725起，北端z2.21，旧水槽台面背面z1.87，因此也有进入净边界的问题，不能只处理两个柜子的交叠。

未实施设想：保留原灶/盆平面中心；北侧台面改x3.81–5.71、z1.905–2.50（1900×595mm），开孔沿原盆550×390mm；西侧台面x3.725–4.335、z2.503–4.87，3mm转角接缝。北侧设封闭盲角、盆下双门检修仓及右窄收纳；西侧地柜按z2.52–3.25备餐、3.25–4.03灶下、4.03–4.85锅具三模块试排，抽屉向+x拉出。需先建真实柜壳/五金占位/收纳并核动态，不凭此设想称可施工。烟机目前约949mm灶面间隔，降低/罩壳衔接须核模型梁窗与机型要求，燃气/电磁和现场排烟仍不明；不擅自增加洗碗机等未选设备。

### 接续：共享软体织物与离线镜面修正

textile-details.js提供确定性缝制枕垫、双面被芯/包边/上翻被口，复用本地CC0织物法线；bedroom-revision、master-dressing、living-revision分别接入，几何随床箱继承变换。旧主卧及沙发软垫仅隐藏留底，框架/占地不变。interior-mirrors给真实镜片登记surfaceRole，GLB导出后Blender按该标记独立复制网格/材质再改镜面，取消vanity父级模糊匹配，避免共享材质污染龙头等物件。无新依赖、无API/水电现场更改，Sites保留既有项目，未发布。回退限定移除上述调用并恢复原软垫可见，不触碰原始GLB。

离线脚本新增非镜片金属处理前后签名比较，发现颜色/粗糙度/金属度变化即报错；当前导出必须包含六片明确镜片。渲染记录同时保存GLB、机位JSON和渲染脚本三种SHA256，三者一致才合并旧视角，避免把不同灯光脚本或机位的图拼成同版。试片采用独立sewn-v2输出名，不覆盖上一版留档图库。

### 接续：主卫设施模块

master-bath-details.js隐藏旧bath2-vanity为baseline，保留根姿态方向并将中心改[12.495,0,2.46]，生成420mm浅柜、真开孔盆及镜侧灯。双抽有侧板/底板/前板和日用品，上抽避管槽加宽避免P弯；给排水模型止于柜内，未画现场接通。原淋浴前玻璃及四个前部组件隐藏，右侧固定侧玻璃/花洒/地漏保留，前片改左固定+右660mm内开门。门登记base/turn/open，复用原自由行走近门交互。PC手机分别打开上/下抽，自动收回另一抽以便观察；同时全开仅保留为压力检查。入口受抽屉占位影响，页面显示“进淋浴前收好抽屉”。原始GLB不改，导出脚本新增主卫动作归位；结构墙门、窗位及机电接点无更改。

### 接续：室内镜面与导出兼容

复用已安装Three.js Reflector，经既有build-render-libs.mjs重新打包，共享vendor/three.module.js，无新依赖。interior-mirrors.js在已确认镜面前表面附着运行时单面反射片；保留背板和尺寸。六片镜面按最多两组同屏更新（公卫两片同组），手机512/PC1024纹理；背向、视锥外、被墙遮挡不更新，遮挡判断短期缓存。镜间递归关闭，其他镜面退为中性背板，不再泄漏HDR；不支持无限互映。候选切换同步清理/重建，导出期间隐藏运行时反射片并恢复可导出的原PBR镜面材质，finally恢复网页显示。verify-mobile-places.cjs增加晾衣定位、收回新增操作并使用导出保护；新mobile-current.glb与机位记录已生成，旧五张PNG仍标留档。没有对结构墙、家具或房间比例作改动。

### 接续：阳台升降与检修动作

design-spec.js新增balconyDrying，balcony-drying.js生成单杆、升降索、三件织物。衣杆收起2550mm、降低1800mm，三件小件按430mm宽/740mm垂距/160mm保守深度；平行玻璃，不在双翼台面上方。主页面新增drying定位及升降/挂衣按钮，手机复用同模型。balcony-care.js取消基站上方浅抽屉、仓内净高839mm；概念检修先开80°上盖、再前抽200mm耗材盒，基站不动。新动作回收即恢复，源GLB不改，无依赖/外部连接或发布；当前交付仍需全屋最终导出和渲染。

### 接续：玄关、公卫外置干区与柜后踢脚

public-dry-zone.js新增integrated并接入当前默认全屋；dryControls暴露台盆柜/镜柜移门动作。柜内设避让中央存水弯的U形层板、毛巾与洗漱用品；管道仅包络示意，不代表已接通。entry-revision.js保留旧门板为隐藏对照，生成六扇约393mm门叶/铰链占位、原尺寸换鞋凳与镜子移位；门叶前移21mm避开原柜体端面，开门扫掠待实际五金核定。finish-details.js从当前可见落地柜包围盒生成柜后扣减，重复生成先释放上一套，避免重复踢脚；门套/阴阳角仍未完成。候选切换时凳位与踢脚同步恢复；无墙门洞变更，无新依赖、设备联网或外部发布。

### 接续：横厅六席使用

design-spec.js统一岛台两席位置[8.02,0,3.62]/[8.02,0,4.48]、旋转π/2；就座较收椅后移50mm，起身后移450mm。dry-island.js将柜深改490mm、抽屉盒深430mm、长边膝深380mm，餐具原尺寸不变且转向两席。living-seat-use.js用真实对象世界位置生成动作及SVG使用矩形；未新增装饰性人体盒子。保留全部拉出不利结果；不得把500mm机门占位当选型机门。无墙门窗、沙发尺度、电视眼位、双翼及阳台柜位变化。网页动作回到“收椅”即恢复初始家具；旧GLB未改、旧高清留档未刷新。

### 接续：卧室整改

bedroom-revision.js集中生成两房的新家具和功能动作，隐藏/改名原家具，不覆盖原始GLB。老人床沿用已验证实墙床位的精确尺寸，增加床箱与窗下矮柜；儿童房使用同一设计规格生成床、柜、桌椅及二维边界。全屋加载后统一接入，旧独立试排页仅保留历史对照。API/网络设备无变化。

最终老人床架1540×2060mm、中心[1.835,0,3.98]，保持床头背面0.805m；长度相较原试排增加30mm，为床垫与床头留位。床箱不是固定轴硬掀：setFraction实现前移0.25*sin(angle)、上翻0.65rad的候选五金轨迹，最大前移约151mm。真实连杆/气撑/锁止尚未选型，不把轨迹采样当制造认证。儿童椅中心由5.31修正到5.00m使收起时深入桌下，拉出450mm仍保留动线；人体矩形尺寸未缩小。

room-facilities.js同步房名/家具宽度和新收纳入口，column-view.js集成动作和门边镜头；旧GLB及旧高清未覆盖。verify-mobile-places.cjs的--no-export分支今后不再写当前渲染相机，报告明确exported=false/renderSource=null，避免把导航验证混作模型导出证据。

continuous-walk.js负责输入、游戏人物圆形足迹、同源mesh投影阻挡、近门交互；column-view.js只集成视角模式、动画更新和UI。无第三方新增依赖、无真实设备连接、无API/数据库。model可见网格生成碰撞体，拒绝使用整个家具组的大包围盒封掉中间通道。动态门/抽屉改变后刷新；帧步长限幅，位移分段以防低帧率穿透。该近似碰撞系统不等于人体工学认证。

沿用当前本地非Git工程，Sites绑定既有项目，未新建站点。保留旧模式可回退；此阶段未发布。

finish-details.js从designWalls与rooms截取干区墙侧，门洞区间剔除，生成40段60×8mm踢脚线。column-view.js初始化默认选择已有master-dressing balanced模型，不改原GLB，不改原房间墙门。手机/PC共用入口。旧高清图明确标注留档，未冒充同版渲染。

## 历史增量：column-flush-power-20260906

- design-spec.js：columnFinish侧20mm/后15mm/前35mm；barDepth=.56、barOffsetZ=.01、storedSeatZ=.375。living-revision.js双翼从饰面侧边起算，各1300mm，不增加结构柱截面。
- bar-work-power.js隐藏旧柱装饰条，保留首个结构mesh。生成薄面板、细检修缝及绕柱空线槽，取消外挂电源板与横梁。新的ExtrudeGeometry台面含真实矩形开孔；两组薄盖独立group，barPowerDebug.setOpen可打开约100.8°。顶部开孔/插座均为未选型示意，没有电气接线。
- column-view.js在bar视点增加翻盖按钮并聚焦电源近景；结构柱定位映射到可见包饰。room-facilities.js增加左右电源定位，living-layout-ui.js/column-check.html同步560mm深、包饰侧面起算及实时净距；手机默认bar。
- 原结构GLB与原墙门不动；本地静态网站，无API/数据库/真实设备或外网发布。回退只还原本增量参数、包饰电源生成与文字定位，保留此前清洁柜等用户修改。

以下为历史记录。

## 当前增量：balcony-care-130-20260906

- design-spec.js定义balconyCare位置[6.20,0,0.89]、转角π/2、1300×600mm、900mm台面，未确认管路。living-revision.js加载覆盖层，不改原home-facilities.glb。
- balcony-care.js确定性生成开孔石材台面、斜壁盆腔、龙头、分隔板、独立存水弯/角阀占位、收纳盆、基站与350mm圆形机器人、浅吊柜和日用品。原落地盆栽识别后缩小移至台面；水电接口保持null。
- balconyCareDebug.setDoorOpen打开90°湿仓门；setRobotOut沿局部+Z（世界+X）移动820mm，属于瞬时位置示意，不是真实导航或完整运动控制。上方开放检修条件待机型。
- column-view.js增加care视点、邻接导航和两只交互按钮；内部设施定位自动打开门。room-facilities.js、mobile-navigation.js同步七个细节及第五张高清图；mobile-preview.html默认打开care。
- living-layout-ui.js所有尺寸来自当前模型包围盒，增加清洁柜平面占位及975mm/310mm尺寸解释。既有桌翼、洗烘错位、岛台/沙发距离保持。
- verify-balcony-care.cjs验证盆腔射线、圆形机器人出口、七定位及按钮；verify-living-revision.cjs把新柜纳入通道障碍，新增台盆站位与机器人释放点连通性；verify-mobile-places.cjs导出前收回机器人并关门，供五张高清图使用。
- 本地静态网站，无API/DB/账号/真实设备接入；未外网发布。回退应仅撤销本增量覆盖层、入口及定位，不覆盖其他用户修改。

以下为历史记录。

## 当前增量：outward-laundry-work-power-20260906

- design-spec.js：固定1300mm双翼，600mm桌深，barOffsetZ=-0.40；沙发2600mm、中心z=4.30。原柱中心、房门、电视85寸和岛台保留；laundryOffsetZ=-0.55，洗烘柜由z=1.24改为0.69，不转向、不给排水赋予虚构坐标。
- living-layout-ui.js：实线绘制新洗烘柜位，虚线保留旧位；实测机门中心与桌后沿错位、柜侧至正面玻璃距离。右侧墙中心被前移的洗烘柜合理遮挡，定位可见性改为额外检测墙体真实暴露表面；仍要求射线首交为目标，不跳过柜体遮挡。
- balcony-side-infill.js：设计覆盖层。原plan.js及窗洞证据保留；2D消费designWalls，3D隐藏window-living-8/10并按同一规格加120mm实体封闭占位，正面window-living-9不改。厚度与材质不是砌体结构规格。
- bar-work-power.js：左笔记本、右可移茶盘/烧水器具；柱外可拆饰面电源盒、两组竖装插座和左侧USB-C选型占位、绕柱空线槽。无承重柱开槽、无地面拖线、无上下水假设。供电点、回路、总负荷、导线、接地、漏电保护、防溅仍待电气专业核定。
- living-revision.js：修正导入圆角桌面的局部轴，缩放local Y改变桌深，避免错误增加桌厚；结构GLB保持原文件。
- living-layout-ui.js、column-check.html、room-facilities.js、mobile-preview.html、mobile-navigation.js同步尺寸、定位和版本。高清图由新导出mobile-current.glb生成。
- 本地静态文件变更，无API/DB/账户/真实设备接入，无外网发布。需要回退时逐项还原本节涉及的参数与覆盖层，不能覆盖其他历史用户修改。

以下为历史记录。

# 本轮实现边界

## 当前实现：four-seats-130-20260906

livingRevision统一1300mm双翼与650mm座位间距；living-revision按真实宽度克隆吧凳，不缩小凳体；支撑仅收至靠玻璃侧，固定/承重未验收。dry-island.js生成1800×900×900mm干式岛台、40mm台面、600×1200mm柜身、两列三层真实抽屉盒体和电源外形；两端300mm腿部凹入。厨房面抽屉单只400mm伸出可演示，返回尺寸面板可收回，不代表五金选定或使用者通行通过。

岛台x=7.47、沙发x=9.425、茶几x=10.55，模型厨房侧1100mm、岛台至沙发1050mm、沙发至茶几450mm；估算眼位x=9.275，屏幕平面距离2565mm、水平视角39.13°。不改变墙门或观影房型。洗烘柜保持原位；右翼桌端820mm是静态值。靠沙发内侧吧椅拉出450mm并按600×650mm坐人占位，后方仅105mm，不能再当后侧通道。以上均为试排，不可下单。

## 2026-09-06 等长双翼与观影试排

design-spec.js 的 livingRevision 是本轮家具参数入口；living-revision.js 对加载的原GLB一次应用400mm柱宽、双翼各1.2m、沙发/岛台/灯具/餐椅/茶几/地毯及主卧空调修订。柱深450mm仍未确认，不改墙体、门洞、房间边界。地毯采用确定性Canvas细织纹理，不调用生成图片推断几何。原home-facilities.glb保留；本轮导出mobile-current.glb供同模型光照图使用。

living-layout-ui.js 从实际显示对象读取包围盒、屏幕与厨房开口所在墙面，显示净距；95cm是岛台到厨房墙完成面，不是164cm门洞宽。估算眼位[9.175,1.08,4.15]至屏幕平面2.665m，至屏幕中心斜距2.733m，水平视角37.84°；这是偏沉浸试排，不声称唯一最佳。mobile-navigation.js增加“沙发坐姿观影”和“横厅尺寸与地毯布局”；保留其他房间定位。

使用状态检查采用25mm网格、600/700mm圆形代理和家具包围盒；从当前主通道[8.32,5.85]出发，检查247个假设洗烘前站位。两种500mm开门包络都只是选型前假设。桌子拉出、占座、机门使用限制明确展示，不用静态桌端净距替代验收。回退仅移除本轮接入/参数及相应预览图，不能还原其他用户修改。没有部署公网，没有新依赖或API。

## 手机版实现边界

在原页面上增加 mobile-view.css / mobile-controls.js，保留桌面工具及原房间定位，不复制一套模型。小屏主工具收为三项和更多菜单；设施/试排面板按需展开，底部房间选择保留。column-view.js 室内环顾使用 pointerId 集合区分单指旋转和双指 FOV 缩放，手势结束/取消清理状态；旋转全景沿用既有 OrbitControls。不支持系统全屏时使用网页内沉浸回退。无新依赖、无服务工作线程、无外部发布；可移除手机样式与控制接入回退，不能回滚此前设计修改。

## 老人房只读试排审计

audit-elder-layout.cjs 读取 column-view 当前 GLB。在诊断浏览器内临时旋转/平移床组，床垫、床架、床头柜和灯具保持原尺寸，检查后还原；不保存修改后的模型。床/柜相交使用实体网格的定向包围棱柱分离轴检查，门扇采用 0–90° 每 1° 采样；几何包络仍非人体动作、连续曲面或实物五金验证。输出 elder-layout-audit.json，基准 app.js、plan.js 和 GLB 不修改。

## 主卧 C 修订实施边界

C 已接入网页选项和定位入口 dressing=balanced，沿用 A/B 可还原机制；不改变默认基准户型。上翻盖 4 mm 前缝、2 mm 两侧缝为几何避让条件，不是五金加工图。验证脚本排除候选床与自身的比较，但仍检查床与其余柜体、房门及边界；翻盖在桌局部坐标系采用旋转包围棱柱分离轴检查，而非直接用世界 AABB 判转动碰撞。基准床几何/材质独立复制，切回原状核对位置及包围盒精确一致。

在既有 master-dressing 模块内增加条件候选，不新建另一套户型。床候选从已读取的原床组复制并独立复制几何/材质后修改指定构件；禁止缩放整屋、缩小床垫宽度或处置原床资源。翻盖用独立转轴和真实台面开口，试排空间与人体占位同源。A/B 原失败对照、原墙门及基准 GLB 均保留；选项恢复必须恢复原床及衣柜。

## 主卧现状审计入口

audit-master-layout.cjs 读取当前加载 GLB 的具名家具、首个实体网格、门与主卧多边形，输出 master-current-layout-audit.json 和主卧现状平面截图。该审计不修改原场景、墙线或 GLB。下一步只基于这份当前证据建立可还原的候选，不复用旧截图中的窗高及家具描述。

已新增 master-dressing.js / master-dressing-ui.js：候选参数从当前 GLB 两柜包围盒和 plan.js 房间边界推导；B 的床移动同时带走其床头柜与日用物子组，不修改外墙、窗或床尺度。候选柜子/梳妆桌由真实侧板、背板、层板、挂杆、分段滑门、抽屉和凳子组成；全部柜门宽度在约 0.78 m 内，仍非五金选型证明。平面取同一对象与状态，床身和床头柜分别绘制；dressingOccupant 统一产生测试及平面粉色使用占位，不能用仅凳子网格不碰床代替人体检验。

比较入口为 dressing=storage/open/original；选择和还原只发生在当前页面内存，基准 app.js、plan-spec 与两个 GLB 均不覆盖。还原床坐标与两柜名称/可见性并释放候选资源。column-view.js 只接入选择、定位、设施名称和照明刷新；design-inventory.js 增加可选 resolveItem，使 B 的床尾浅矮柜不再被清单误叫到顶衣柜。无外部 API、依赖、发布或真实智能设备操作。回退只去除本轮候选模块及接入口，不能回滚公卫、门或窗高修正。

## 公卫干区可逆候选

新增 public-dry-zone.js，依赖现 plan.js 的后墙/公卫侧墙/老人房墙线定位候选外框。1000 mm 是公卫墙面至上方分墙侧面的投影对齐范围，不能理解为干区右侧存在一堵墙。镜柜、柜侧与底、两抽面、盆底/四边、台面开口、龙头、侧挡/玻璃框、手巾/皂瓶为真实网格；镜面为近似材质，尚非照片级。玻璃 12 mm 是渲染厚度，非安全玻璃采购规格；抽屉导轨、存水弯、管路及固定未建成安装详图。

column-view.js 仅在明确点选候选时暂藏原 bath1-vanity、换名并添加候选；恢复时归还原名/可见性，释放候选几何与材质。不导出或覆盖 home-facilities.glb / home-scene.glb。默认无 dry 参数时保留原状。局部尺寸平面读取同一候选包围盒、墙线及门扇数据；站位与抽屉矩形从同一候选参数生成。dry=deep/slim/offset/original 可直达，候选还原无 API、依赖、外部发布或全屋基准变动。图上方不是已确认北向。

验证工具基于直径 600 mm 圆形代理、25 mm 网格、当前门开 90° 的局部入口到公卫路线；站位固定 600×600 mm，开抽屉假设拉出 320 mm 且站位向后退同样距离。墙段按当前开口拆分，家具及门片读取实际网格包围盒。此保守简化不含动态身体转向、轮椅、门实际安装误差或全屋路径。回退限于新增候选模块、当前页的候选入口及验证，不修改原 app.js 或 GLB。

## 本轮：统一窗高证据与模型

window-profiles.js 保存 7 组窗区、11 段窗面的原图线索引、人工视觉读取 LD/CH 和阳台用户改造覆盖值。不是 11 扇开启窗。app.js 按同一 profile 生成玻璃及三卧窗上下非透明边界；glazing 返回具名组与元数据。厨房暂用透明玻璃、卫浴暂用隐私玻璃，仅材质示意非玻璃订货。export-spec.mjs 将有效 sill/height 与 source_sill/source_height 同步到 plan-spec.json；不改原横向几何。

audit-source-windows.py 保留完整原 PDF 光栅、主卧/儿童房标注放大、原始线条来源、窗高表和四条平行窗线的跨度范围。该范围不是净开口或安装宽度。source-window-check.svg 不作完整结构图。当前旋转页新增逐房窗高定位及 loadedWindowAudit；缓存 window-audit，重导出仅 home-facilities.glb，旧 home-scene.glb 保留。回退仅限本轮窗高 profile 与相关同步，不重置目录。

## 本轮：儿童房试排模块

audit-child-layout.cjs 只读浏览器源场景，提取床底、衣柜柜身等实际网格包围盒，输出 child-current-layout-audit.json。child-layout-options.js 从既有原图尺寸核对 JSON/门叶 JSON 推导主要矩形范围与门叶铰接局部坐标；候选家具外框为明确标记的设计值。原模型对照用其实际门片位置，不混入校准后的候选门位。

children-layouts.js / html / css 提供独立条件比较页，二维 SVG、三维体量与重叠检查共用 makeChildStudy / activeItems 数据；门每 1° SAT 抽样，候选采用 900 mm 预留半径而非成品门值。600 mm 圆形占位、25 mm 网格四邻接路径只做方案试探，固定门开 90°，不是人体或无障碍标准。椅子收在桌下的重叠明确单列，三维桌面/腿分开表现。全屋页新增入口和已确认冲突文案，不改变全屋墙体、家具或 GLB，无新依赖、外部 API 或发布。

若回退试排，只去掉全屋页的试排入口及本轮独立模块，不回滚已有房名、主卫修复或门扇修复。预算未给定；本轮只列功能/数量差异，不给假报价。

## 本轮：主卫柜与定位索引

app.js 将 bath2-vanity 从 (1304,356,-π/2) 改为 (1295,356,+π/2)，台面保持 670×490 mm，并登记真实柜体碰撞框。只重导出 home-facilities.glb；旧 home-scene.glb 不动。room-facilities.js 同步正面相机和未验证项；column-view.js 从 plan.js 墙线/门洞、房间多边形和实际导入对象包围盒生成索引，不另画假房间。点击主卫洗漱台统一切到主卫，避免仍显示主卧标题。缓存 washroom-clarity；无外部 API、依赖或管路变更。回退限于这些定位与柜体差异，不重置其它工作。

## 门扇修正实现

补充：共享 door-motion.js 同时用于 app.js 与 column-view.js，重设完整欧拉角避免导入 GLB 后保留 π 的 X/Z 旋转。旋转页在选中门设施后出现独立开合按钮，支持 focus=door。模型缓存版本 source-doors，页内设施列表和待完成清单提示儿童房柜体冲突候选。框边退让45/27.5 mm 是避免网格贴墙的方案几何，不是现场门框参数。

在唯一现行 plan.js 增加 doorLeaves（原门扇索引、铰接端、转向、双扇比例），app.js 使用同一门洞端点创建门片。每片独立开合，统一 setDoorLeafOpen 供初始状态与点击复用。替换旧固定 +0.48π；改为方向明确的 90°。面板用门组名称关联，移除对象循环引用，便于 GLB 保留元数据。只重导出 home-facilities.glb，不覆盖旧离线图使用的 home-scene.glb。回退应按本段差异处理，不重置整个目录。

## 门洞证据提取

新增 `audit-source-openings.py`：校验原 PDF 哈希后读取明确的原门弧、矩形叶片、门框和墙端对象，输出 `source-opening-schedule.json` 和 `source-door-check.svg`。坐标保留 PDF 原始 x/top；展示旋转不冒充北向。原图没有单门尺寸时只给推导值。未改 plan.js/app.js/GLB 或现有模型；没有外部接口与依赖变更。

## 原图尺寸核对，不更换模型

`audit-source-dimensions.py` 用 pdfplumber 读取原 PDF 尺寸界线，按人工视觉核对的索引关联 4 条总链、35 个分段；读取当前 `plan.js` 计算 6 处模型跨度，读取 CAD 诊断墙 246088/252126 推导局部净跨度。输出 `source-dimension-audit.json`（带输入哈希）。不写原 PDF/DWG，不重跑旧提取器、不改坐标或导出 GLB。资料/差异/测量门槛见 `基础尺寸与待确认清单.md`。没有新增运行依赖；使用本机已提供的 Python/pdfplumber 和 Node。

## 房间与洗漱定位入口

`column-view.html/js/css` 新增可关闭的空间定位对话框，复用 `room-facilities.js` 的对象名、相机位置与现状说明，不另造几何。`plan.js` 将 bath1 展示为公卫（次卫），bed1/bed3 保持老人房/儿童房。新增 `focus=public-vanity` 和 `guide=1` 入口；页面模块版本 room-clarity。无模型重导出、无依赖或外部接口变更。回退仅移除这组界面入口及绑定，不覆盖用户模型。

## 玄关查看与软装清单

`app.js` 在现柜位置构造空心侧板/层板/三扇门；同一玄关组包含柜、凳、镜。`everyday-details.js` 只接收原家具对象，在其台面或柜内放日用模型，不建立另一套户型。柜顶降至 2.60 m，避免旧 2.77 m 柜体穿过 2.62 m 吊顶；上部封板未按实测设计。

`column-view.js` 增加鞋柜入口及可恢复门板剖视。正立面从前景厨房一侧观察，使用 z=5.55 m 临时剖切隐藏遮挡，并显式标注不是实际站位；离开该查看项自动清空剖切，离开玄关恢复门板。可见性检查同步排除隐藏网格与剖切外网格。顶部高度由 ResizeObserver 同步，兼容新增按钮换行。

`design-inventory.js` 从 `room-facilities.js` 生成分房清单，按加载的同源 GLB 检查对象是否存在，点击聚焦实物；未完成项独立显示。`plan.js` 保留 bed1/bed3 ID 及坐标，仅将展示名改为老人房/儿童房。电视入口由对象名查找，避免软装条目插入后原数组索引失效。无外部连接、新依赖、真实设备控制或原图改动。回退仅逆向相关差异并重新导出设施 GLB，不覆盖其他用户改动。

## 次卫蹲便、淋浴入口与铝扣板浴霸

`app.js` 保留蹲便中心原定位、移除次卫坐便网格；盆缘有真实孔、旋转曲面凹盆、双侧脚踏、冲水箱。仅在显示地面及下方饰面上按同一定位避让盆体，不创建已确认结构沉箱或排污管。`shower(...,true)` 将次卫门置于朝入口侧、向内打开 90°，花洒转向后墙；主卫入口方案不变。

`services.js` 保持既有两卫网格及标高，在 300×600 mm 模块中分别建暖风百叶、排风吸口、4000 K 照明、上部机身，板块避让设备并补四周收边。不虚构外排管、外墙开口或防水施工。`room-facilities.js` 同步对象入口及精度提示，`focus=squat` 直达次卫；HTML、设施模块与 GLB 更新版本键。无外部接口、依赖、原图文件变更。回退按本次局部差异逆向、重新导出，不覆盖其他家具或旧离线 GLB。

## 最新：餐边柜四段构造

`app.js` 将被否定的两排上柜合成一排，在台面下新增 `sideboard-drawer-bank`：六张斜切抽面与六个独立底板/侧板/背板盒体。下柜门缩短，抽屉与柜门分开；中横板前缘退至手扣区后。抽面与柜门均以截面挤出生成实际 45°坡面。上柜采用底板、顶板、分隔板及下延门板，手扣区不被底板填死。台面实体前后 600 mm，柜面内收由真实坐标决定。管线机机身、面板、接水区保留原坐标与名称。不新增抽屉开合动画或声称已验证滑轨。

`column-view.js` 加入 `focus=sideboard`；设施条目提供整体、下延门和斜切门细节入口。整体相机前移避免原设备吊顶遮住柜体上沿，不移动吊顶来掩盖实际标高。继续同源导出设施 GLB，不改旧离线 GLB、不上传原图。无新增依赖或外部接口。回退仅逆向此轮差异后重新导出，保留用户其他更改。

## 最新：横向留白方案

`app.js` 删除上一版展示格，沿原背界构造 120 mm 饰面：饰面和屏幕共同后退 320 mm，结构背界不变，网格齐平保持 0 mm。矮柜主体 3300×380 mm（含面板总深约 390 mm）、台面高 478 mm，摆件在实体台面上、饰面前且低于屏幕。柜体最前沿不超旧占地。电视机厚度、支架、线缆与散热安装未选型。

缩小茶几、沙发深度及扶手，岛台两端各一常用椅；`design-spec.js` 统一三席吧椅坐标，由源场景及当前柱位查看器共同引用。`services.js` 将客厅开关跟随薄饰面移到实心端面。`column-view.js` 删除展示格补光，改低柜柔光；增加电视墙快捷入口，可见性检测同时抽样实物子网格，避免组合摆件中心落在空隙造成误判。界面文案更新，未加广角或改相机常规参数。

只覆盖同源导出的 `home-facilities.glb`；不动旧离线 `home-scene.glb`。旧自由漫游柱位仍单独标注。无新增依赖、外部上传或设备控制。回退仅逆向本轮相关差异并重新导出，不做目录级覆盖。

## 最新：电视墙三格展示区

`app.js` 将电视饰面局部实心块替换为边框、木背板、层板和三个真正空腔，加入陶瓶、书与环形摆件及发光条；保持局部 z=-0.23..0.20 m 饰面范围，矮柜最前沿仍为 0.24 m。`services.js` 仅将客厅开关改到电视柜实心端面，避免穿入展示格。未改结构墙或原图纸。

`room-facilities.js` 增加整墙及展示格定位；`column-view.js` 支持 `focus=tv` 直接打开整墙视角，默认收起设施面板、不画包围框，并加入格内近距离暖光模拟。导出至 `offline-render/home-facilities.glb`，不覆盖旧离线 `home-scene.glb`。无新依赖、API 或外部上传。回退仅逆向本节对应代码差异并重新导出，不覆盖其他改动。

## 最新：全屋配色与照明

`design-spec.js` 统一配色、饰面描述与 4 盏顶灯的方案位置/尺寸/光色。`app.js` 应用墙面、柜门、布艺和五金颜色，构造三卧柔光吸顶灯和厨房线性顶灯；公共筒灯保留 10 处但重新分配为公共区、过道和水槽任务照明，卧室不再叠加原零散筒灯。灯具是同一源场景里的网格，不是网页贴图。

`realistic.js` 将既有木纹调整成低对比自然木色，降低石材表面反光；地面保持 800 mm 网格，改低纹理浅暖灰、同色缝，roughness=0.78。此值是渲染参数，不是防滑认证。干区墙面配色不表示淋浴湿区可以用普通乳胶漆。

预览读新导出的 `home-facilities.glb`，新增顶灯对象进入设施列表。日夜按钮切换太阳、环境亮度及室内补光；新主灯补光点跟随同一灯具规格，厨卫光色与卧室区别显示。WebGL 光照是显示近似，不把灯光数值当作实际照度/流明验收。

左卧与次卧观察点调整以避开门扇遮挡；只修改视点，不移门或床。厨房顶灯前移，避开原烟感示意位置；不宣称符合最终烟感安装间距。无新增依赖、外部接口或真实智能设备控制。旧离线图/旧 `home-scene.glb` 不覆盖。回退按本轮差异处理，不回滚原用户更改。

## 最新：房间设施与局部剖切

- `app.js` 给床、衣柜、书桌、三台挂机、厨卫设备分配稳定名称，并在原家具位置范围内补设施细节；厨房水槽台面/柜体为盆体让出实际孔洞。原房间墙线不变。
- `export-render-scene.cjs --facilities` 输出独立 `offline-render/home-facilities.glb` 和 `facilities-cameras.json`；不覆盖旧 `home-scene.glb` 和离线图来源凭据。新 GLB 仍由原主场景导出；预览继续执行已有柱位与吧椅修正。
- `room-facilities.js` 绑定 47 个对象名称、方案说明与少量观察位置，不新建房间坐标。`column-view.js` 点击条目用真实对象包围盒定位；未找到对象时禁用按钮。截图中的绿色框只作选中反馈。
- 本房布局：依据 `rooms[].poly` 计算相机和裁剪范围，1.35 m 水平剖切；从同一多边形生成遮罩隐藏不属于本房的相邻空间。视图复原时移除裁剪、恢复吊顶。属于显示处理，不是拆改墙体或真实缩放户型。
- 设施面板在画面内常驻、可折叠；箭头避让面板。小屏初始收起面板，仍可展开操作。`space`/`mode` 查询参数只接受既有房间/模式白名单，可直达厨房布局。

未新增 npm 依赖、外部服务、真实设备连接或图纸上传。未更改原 PDF/DWG。若撤回本次功能，以本轮文件差异回退，并保留此前柱位与座椅改动；不要覆盖整个目录。不能将旧版自由行走的净宽检查外推为新版柱位全屋通行通过。

## 画面箭头及吧椅修订（优先于下方历史隐藏方案）

`column-view.js` 从 `plan.js` 的 rooms/P 派生房间相机，不另建户型；客厅及阳台保留校对视点。DOM 箭头按目标相机点投影，视野外目标使用画面边缘虚线按钮；这是空间切换标签，不是门洞或无障碍路径指示。前后箭头按 15 个查看点循环，下拉选择可直接定位，说明栏收起或全屏时仍可用。页面内明确非连续行走。

从同一 GLB 恢复四个原软包吧椅组，仅在校对预览设置世界中心为 x=8.12/8.62/10.10/10.70、z=2.18 m，朝向湖面，柱及两翼仍按用户纠正示意。既有脚踏、靠背、软垫保留。校验仅针对闭合家具水平占地；不外推拉椅坐人和通行净宽。原 `app.js` 自由行走与离线图仍旧版，不冒充已同步。

本次未引入依赖、API、数据库或外部服务。回退需按文件差异撤销本轮导航和座椅定位，保留用户既有改动；不覆盖整个目录。

大幅预览：新增 column-view.html/css/js，加载当前 GLB（同源墙体/家电/家具），只将命名柱体与双翼桌按 columnCorrection 平移；4 张随桌平移会产生冲突的吧椅仅在校对预览中隐藏，并显式告知。不是新版可行走碰撞场景。OrbitControls 与 GLTFLoader 来自既有 Three.js 依赖，更新本地 bundle，无新增依赖或外部上传。固定点环顾使用相机旋转，绕场景模式使用 OrbitControls；静止时 SSAO，拖动时基础渲染改善响应。旧 column-check.html 无 plan 参数时转入新入口，plan=1 保留原对照图。

最新柱位纠正：`plan.js` 增加 `columnCorrection` 用户证据，`column-check.html` 从同一墙线数据绘制旧/新柱位对照。现有 `notes.column.point` 保留为旧场景坐标，家具冲突解决前不默默挪柱并生成失真的成品。校对页明确该边界，原图纸未改。

## 同源离线渲染与双模式页面

- `laundry.js`：洗烘套装及完整三维柜体的本地构造，机门、滚筒观察窗、控制区、叠放架、吊柜和侧清洁柜。`app.js` 接入并注册实体碰撞，`plan.js` 新增仅查看导航点；墙体、房间多边形及门洞不变。
- 洗烘柜处上下水/电源未有证据，元数据显式为 null；不绘制虚构排水管路。旧版离线批次已暂停，完成图保留，但不视为包含新洗烘柜。

- `export-render-scene.cjs`：从当前网页 scene 导出 GLB，onlyVisible，摄像机位置来自 rooms/P；保持房间、家具和门窗同源。导出后 geometry 更改需重新导出和渲染。
- `render-home.py`：本地 Blender 4.5.9 LTS / Cycles CPU 离线渲染。导入同一 GLB，添加用于视觉表现的窗光与室内光，读取本地 HDR；不另建墙体。渲染灯能量是艺术参数，不是电气负荷或照度设计。
- `offline-render/renders.json`、`renders-still.json`：每张出图后才写入清单，记录分辨率、采样数、源 GLB SHA256；两类作业分开写，避免相互覆盖。
- `showcase.html/css/js`：读取已完成清单。球面贴图用于全景拖动/缩放，高清静帧可打开原尺寸；与自由行走区别明确，不能在全景中连续平移。
- 主页面使用模态 iframe 打开展示；暂停隐藏的自由行走渲染，关闭时保留原行走位置。跨框关闭消息检查 origin 和 source。
- `?view=effects` 直达高清/全景模态，仍保留原自由行走入口。模式列表只显示该模式计划交付的视角，避免把另一模式专用视角误标成缺图。
- HDR 在实时端水平移位半幅，离线世界纹理旋转 180°；清单显式记录 `environmentRotationDegrees:180`，页面排除旧的错误朝向图。
- 玻璃使用命名材质，离线转换为透射 BSDF；窗光代理对相机、镜面和透射射线隐藏。该光照/材质转换不改变几何，不作为玻璃规格或现场反射率证明。
- 宽度不足 2000 的样张使用独立 `-sample` 文件名且不入清单，避免覆盖已发布高清图。全景/静帧分别保存 home-panorama.blend / home-still.blend。

工具从 Blender 官方下载站获取可移植包至 D:/Installers/Blender，SHA256 校验后解压到 D:/Tools/Blender，不改系统 PATH。无付费服务、无图纸上传。

## 材质与光照升级

新增 realistic.js：本地 PBR 贴图、HDR 环境、台面低对比纹理、柜下区域光。最终实时管线为 RenderPass → SSAOPass → OutputPass，动态场景每帧更新，可关闭增强模式恢复基础渲染。

最终依赖仅 Three.js 0.160.1 和构建用途 esbuild 0.25.9。测试过 three-gpu-pathtracer 0.0.23，但未通过出图时限验收，已卸载该包和 three-mesh-bvh；当前没有未工作的精细采样按钮。bundle 将 Three.js external 指向现有 vendor/three.module.js，防止双份引擎。原始图纸未改动。

冰箱外包围尺寸不扩大；增加一个复用既有可达位置的只看视点，当前共 13 点。柜体倒角保留标称包围尺寸。SSAO 只近似局部遮蔽，不能等同物理全局光照、施工或照片级效果。

- `plan.js`：保留坐标、墙体、开口及家具布局参数，只更新证据状态和过时文案。
- `app.js`：为保留柱增加占位与禁拆元数据；两翼桌下加发光条与脚踏杆，均不超出现有桌面占地。
- `services.js`：局部吊顶材质切换；展开时显示隐藏内机及室内空气关系示意，恢复时隐藏。蓝色代表送风，金色代表回风，示意曲线不作为风管施工几何。提供 `setCutaway(boolean)` 与只读 `state.cutaway`，仅供本地界面/验证。
- `index.html` / `style.css`：展开按钮、阳台定位、CAD 核对入口；修复 681–1150 px 下右栏完全隐藏导致按钮不可访问的问题。
- `verify-services.cjs`：增加展开/恢复、柱与双翼细节、响应式回归。沿用现有导航、净宽和设备检查。

无业务 API、数据库、Redis、MQ 或账户变更；新增的本地素材与构建依赖见上文。Three.js 静态资源仍在本地加载。`hvac.outdoor` 和 `pipeRoute` 均保持 null。

运行：沿用 127.0.0.1:8768 静态服务，刷新页面加载。非生产发布。回退交互时可隐藏展开入口并默认完整吊顶；任何源文件回退应先保存当前副本并按文件差异操作，不使用目录级覆盖或删除。
# 本轮增量：手机定位与阳台视点

### 2026-09-07 独立设备落位研究

### 后续：侧向洗漱独立对照页

半开放修订：study.screen.x1由3.8改为3.088，面板、湿侧饰面、底边均从同一screen对象构建，防止残留长屏。study.corridor与standing分别驱动SVG和3D辅助显示。可用view=overview/plan直达视角；手机保留旋转与切换。原GLB、plan-spec.json及公开站点未改；此页仍为本地候选。

entry-wash-study.html/js从原current-design-v2.glb和plan-spec.json只读载入；原墙门未写回，单一study对象驱动新几何和平面图。复用public-dry-zone.js并在当前页注册独立选项，旋转至公卫外墙，原凳/镜/旧鞋柜仅在本页候选模式隐藏。新鞋柜无坐位、底部180mm镂空。页面可切换原方案、实视、局部剖切、尺寸图及墙透明度。没有发布、购买、管线施工或原数据覆盖。回退直接使用原column-view.html。700mm洗漱净宽不等于台盆前另有700mm，尚未满足舒适使用证据。

`appliance-revision-20260907.py`读取原`offline-render/current-design-v2.glb`，复用现有Cycles渲染器；在内存中缩短餐边柜右端上部面板，添加650mm设备塔及蒸烤箱规划包络。单侧岛台替换为同占地的双侧对坐研究结构，复制原椅子到对侧。原GLB、网页和户型JSON不写回。现有渲染器仅新增显式`--appliance-revision`入口，不带此参数仍走原几何路径。输出单独Blend、PNG及审计JSON。没有外部发布或智能设备连接；回退只需继续使用原网页。

mobile-navigation.js 只消费既有 room ID、facilityRoom、设施对象与 visit/focus 回调；新增五个常驻卧卫按钮和空间/设施弹层。手机隐藏旧下拉和冗长说明，桌面原控制保留。阳台新视点由 columnCorrection 与护栏线推导，只变相机，不变墙、柱、桌子及坐席；补齐箭头邻接。高清光照试图须来自当前已纠正柱位模型，不复用旧 home-scene.glb。无 API、数据库或智能设备接入变动。

## 2026-09-09 主卧整面柜墙与圆弧挂衣端

交付沿用 master-suite-plumbing-study.html 与同名JSON；修改 master-suite-details.js、master-entry-storage.js，原结构坐标不变。整墙总355cm＝60cm开放端＋295cm推拉柜，总深按用户追加改65cm；床框暂排188×200cm，对应180×190cm床垫，模型床尾净距66.9cm。衣帽区100cm妆位＋150cm衣柜。无远端、后端、DB、MQ或配置发布。局部源码规则使用 develop-and-verify，目录为独立静态交付，Git元信息检查被环境钩子阻断，不声称分支状态。旧图保留为历史证据，不创建新旧混用的渲染入口。

## 2026-09-09 柜侧浅位与半高床头背景

当前规格 half-head-side-niche-27：侧面圆角孔50×110cm、内凹12cm，孔在YZ面且只削去局部侧板；储物层板避开侧位，正面三个移门覆盖全柜宽。床头实墙宽280cm范围内加约2.4cm薄护墙板及4cm深顶收口，完成高度124cm；上半保留原墙，矮软包至98cm。只改家具/饰面，同一JSON驱动，不拆原墙、不改房门。柜体侧孔承力和移门实物五金仍待深化。

## 2026-09-09 入口柜角斜切圆弧纠偏（28版）

curved-cut-corner-28：entryCorner的同源多边形驱动SVG和3D挤出，上下柜及层板均沿斜切轮廓收口；删除旧coatAlcove及外套模型。闭柜305cm调整三扇门及内部层板，转角50cm。转角封闭部分仅为体量方案，实际柜门五金待深化；路径检查沿用保守外接矩形，不以斜切额外面积夸大通行能力。

## 2026-09-09 套间一体暖色优化（29版）

版本warm-unified-29，沿用现有HTML、主渲染、details、entry-storage和JSON。仅本地静态交付；D:/tep-chat不属于Git仓库，不宣称branch/提交状态，不涉及任何Chaken外部操作。入口层板80/140cm，上柜底193cm；整段侧背板接齐。梳妆镜YZ面圆角半径6cm，保留原滑动包络；取消台后小阶，上柜进深收齐相邻柜前，腿部净空不改。程序生成细织物凹凸纹理；日夜切换调节环境与局部灯光，仅为视觉表达。无API/DB/MQ/远端影响；恢复既有28版相关文件可退回外观。

## 2026-09-09 独立方案一：高低柜墙

独立新增master-suite-lowwall-study.html/js、master-suite-lowwall-details.js、master-suite-lowwall-spec.json及说明、局部测试；只读复用原entry-storage、geometry与vendor。使用同源坐标生成2D/3D，未修改原版主页面/主脚本/details/JSON。高柜各两扇40cm平开门，中段低柜拉抽35cm；真实物理承力、门五金选型及舒适性仍待核。当前为独立静态交付，无Git分支或外部环境发布。回退只需返回原页面，不需要改原文件。

## 2026-09-09 整面柜与柜前升降幕布（30版）

cabinet-cinema-30新增master-suite-projection.js，JSON增加projection和L8/P2设计候选，主页面增加收放幕布和洗柜光控制；无外部设备联动。幕面暂80英寸16:9（177.1×99.6cm），黑上边32cm，柜前11.9cm；幕盒220×14×12cm为选型预留，不是产品数据。画面程序生成，仅作幕面示意。幕盒/柜体与灯槽互不重叠，需独立固定至核定基层、电源端可检修；实际电机控制、防夹限位、镜头偏移、投射距离/比、遮光与散热未验证。渲染改动采用accurate-floorplan-3d同源几何及develop-and-verify局部验证，不涉及Chaken目标或Git发布。

## 2026-09-09 窗帘设备补充与空调撤回（31版）

curtain-ac-31增加equipment同源参数与master-suite-equipment.js；平面/3D联动，纱布独立开合，可剖开检修面看双电机和预留电源。布帘盒宽20cm、纱帘盒宽10cm、高18cm均为候选；电机收于头端，避免书柜顶与末端电机冲突。布帘到60cm通路包络名义余量3.6cm，闭帘不可使用飘窗位，摆动/窗扇/遮光/曲轨选型待核。AC status=withdrawn，隐藏机体/风向/电源和平面标记，未确定替代位。投影吊装、锚固、电气与智能控制仅设计模拟。未改门墙、床柜；无外部部署或硬件操作。

## 2026-09-09 床尾墙挂机与柜体适配（32版）

沿原独立静态页面实现footwall-ac-32，不在Git仓库，无分支或外部发布。JSON中高柜缩至185×65cm；新浅柜120×35×85cm，独立推拉控制，门板/背板/层板具实体厚度。主卫相邻墙空调仍废弃，候选挂机改在foot-wall，85×25×30cm，底210/顶240cm、顶距20cm皆基于暂定260cm层高。矮柜到机底125cm；前方拆洗范围仅代理包络，无气流/安装验证。80英寸幕面中心及投影镜头同步至x=3.225，避开空调正前方。更新details/equipment/projection/main/HTML和局部测试；不存在API/数据库/硬件联动。减少120cm宽通顶柜，不承诺容量不变；既有150cm衣帽柜保留。回退需同时恢复这组同源参数与渲染文件，不能只恢复平面。
